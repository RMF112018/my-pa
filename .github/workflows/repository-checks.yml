name: repository-checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: repository-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    # 30, raised from 20 on 2026-09-03. The FAST selection this job runs grew to
    # 16,380 tests. Across six runs in under twenty hours (2026-09-02 20:38 to
    # 2026-09-03 15:58) this job's wall clock measured 19m43s, 19m42s and 19m36s
    # (1687b634, 5cc645d8, 251a495c), then timed out at 20m02s and 20m18s
    # (9c1e463b, 53797f5b), then ran 14m45s at 8f981f1c while the dependency-floor
    # job took 20m39s. A timeout that fires on a green suite reports a red job for
    # a reason no test names; the value is sized to the measured duration, not to
    # a guess.
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Validate repository documents and configuration
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          import sys

          root = Path.cwd()
          errors = []
          link_pattern = re.compile(r"\[[^\]]*\]\(([^)]+)\)")

          for path in root.rglob("*.md"):
              if ".git" in path.parts:
                  continue
              text = path.read_text(encoding="utf-8")
              for target in link_pattern.findall(text):
                  target = target.strip()
                  if not target or target.startswith(("http://", "https://", "mailto:", "#")):
                      continue
                  clean = target.split("#", 1)[0]
                  if not clean:
                      continue
                  resolved = (root / clean.lstrip("/")) if clean.startswith("/") else (path.parent / clean)
                  if not resolved.resolve().exists():
                      errors.append(f"{path}: missing relative link {target}")

          required = [
              root / "AGENTS.md",
              root / "CONTRIBUTING.md",
              root / "SECURITY.md",
              root / ".github" / "pull_request_template.md",
              root / ".github" / "ISSUE_TEMPLATE" / "bounded-change.yml",
          ]
          for path in required:
              if not path.exists():
                  errors.append(f"missing required governance path: {path.relative_to(root)}")

          if errors:
              print("\n".join(errors))
              sys.exit(1)
          PY

          ruby -e 'require "yaml"; ARGV.each { |f| YAML.safe_load(File.read(f), aliases: true); puts "validated #{f}" }' \
            .github/ISSUE_TEMPLATE/bounded-change.yml \
            .github/dependabot.yml

      - name: Run Python FAST tier when package configuration exists
        shell: bash
        run: |
          if [[ ! -f pyproject.toml ]]; then
            echo "No pyproject.toml yet; governance/configuration validation is the applicable gate."
            exit 0
          fi

          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path

          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService

          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
          python -m ruff check .
          python -m ruff format --check .
          # `mypy` with no path argument, deliberately: an explicit target
          # overrides `[tool.mypy] files` in `pyproject.toml`. Measured at
          # `bcdbf6d`: `mypy src` checked 93 files and a bare `mypy` checked
          # 109, so the 16 in `apps/` and `migrations/` were type-checked by
          # nothing in CI. Proven with a plant: a deliberate `-> str`
          # returning `int` in `apps/` left `mypy src` reporting "Success: no
          # issues found in 93 source files", and the same plant in
          # `migrations/env.py` did too, while a bare `mypy` caught both.
          # Both invocations are bare now, so they agree by construction
          # rather than by a number that has to be maintained. `D-64`.
          python -m mypy
          python -m pytest -m "not slow and not database and not network and not connector and not evaluation and not e2e and not recovery"

      - name: Package governance candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: governance-candidate-${{ github.sha }}
          path: |
            .ai/project-sources/00_AEOS_MASTER_INDEX.md
            .github/CODEOWNERS
            .github/ISSUE_TEMPLATE/bounded-change.yml
            .github/dependabot.yml
            .github/pull_request_template.md
            .github/workflows/repository-checks.yml
            AGENTS.md
            AI_OPERATING_MANUAL.md
            CLAUDE.md
            CONTRIBUTING.md
            SECURITY.md
            docs/00_REPOSITORY_SOURCE_INDEX.md
            docs/governance/GOVERNANCE-AUDIT-MYPA-MCV-20260730.md
          if-no-files-found: error
          retention-days: 7
          include-hidden-files: true

  web-security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: web
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install locked web dependencies
        run: npm ci

      - name: Verify the web security boundary
        run: |
          npm test
          npm run lint
          npm run typecheck
          npm run build

  dependency-floor:
    # **Twenty minutes, not ten.** Both this job and `validate` run the whole
    # FAST tier, which has grown to 8,419 collected tests. At ten minutes
    # `validate` was finishing in 8m24s and this job was cancelled at 10m16s --
    # a budget set when the suite was smaller, failing a tree that was green.
    # `database-tier` is an aggregator over split lanes; each lane sizes its
    # own timeout. A tier's budget is sized
    # to the tier. Guard cost was cut in the same change that raised this, so
    # the headroom is real rather than spent on arrival.
    #
    # `pyproject.toml` declares a lower bound for every runtime dependency, but
    # an ordinary install resolves the newest release, so the bound is an
    # assertion nothing checks. This job installs the declared minimum and runs
    # the suite against it, which is the only way the claim stays true.
    #
    # It is not hypothetical: a recursive PEP 695 type alias works on current
    # Pydantic and raises PydanticSchemaGenerationError at the declared floor,
    # where the suite fails during collection while the validate job stays
    # green. `src/my_pa/contracts/v1/envelope.py` avoids that form for this
    # reason, and this job is what keeps that reason true.
    runs-on: ubuntu-latest
    # 30, raised from 20 on 2026-09-03. The FAST selection this job runs grew to
    # 16,380 tests. Across six runs in under twenty hours (2026-09-02 20:38 to
    # 2026-09-03 15:58) this job's wall clock measured 19m49s, 19m10s and 19m41s
    # (1687b634, 5cc645d8, 251a495c), then timed out at 20m15s and 20m17s
    # (9c1e463b, 53797f5b), then ran 20m39s at 8f981f1c, the run that made 30 the
    # value for this job as well as for validate. A timeout that fires on a green
    # suite reports a red job for a reason no test names; the value is sized to
    # the measured duration, not to a guess.
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install the declared minimum runtime dependencies
        id: floor
        shell: bash
        run: |
          if [[ ! -f pyproject.toml ]]; then
            echo "No pyproject.toml yet; no dependency floor to verify."
            echo "applicable=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path

          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService

          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY

          # Derive the pins from the declaration rather than restating them, and
          # require a floor for every runtime dependency. Parsing with
          # `packaging` rather than a regex matters: a hand-rolled pattern
          # silently drops `pkg[extra]>=1.2`, `pkg<3,>=2.7`, and `pkg~=1.2`,
          # which would leave a dependency untested with the job still green.
          PINS="$(python -c '
          import sys, tomllib
          from packaging.requirements import Requirement

          specs = tomllib.load(open("pyproject.toml", "rb"))["project"]["dependencies"]
          pins, undetermined = [], []
          for spec in specs:
              requirement = Requirement(spec)
              lower = next(
                  (c.version for c in requirement.specifier if c.operator in (">=", "==", "~=")),
                  None,
              )
              if lower is None:
                  undetermined.append(spec)
              else:
                  pins.append(f"{requirement.name}=={lower}")

          if undetermined:
              print(
                  "no lower bound declared for: " + ", ".join(undetermined),
                  file=sys.stderr,
              )
              sys.exit(1)
          print(" ".join(pins))
          ')"

          if [[ -z "$PINS" ]]; then
            echo "::error::No runtime dependency declares a lower bound to verify."
            exit 1
          fi

          echo "declared floor: $PINS"
          # shellcheck disable=SC2086
          python -m pip install $PINS

          # pip exits 0 after a forced downgrade even when it prints a conflict,
          # so ask explicitly. Without this a dev dependency that outgrew the
          # runtime floor would surface as a confusing floor failure.
          python -m pip check

          python -m pip list | grep -Ei '^(pydantic|my-pa) '
          echo "applicable=true" >> "$GITHUB_OUTPUT"

      - name: Type-check and test at the declared floor
        if: steps.floor.outputs.applicable == 'true'
        shell: bash
        run: |
          # Import the contracts, not just the package root: `my_pa/__init__.py`
          # touches no Pydantic model, so importing it alone would succeed even
          # on a build that cannot construct a schema at the floor.
          python -c "
          import my_pa
          from my_pa.contracts.v1 import ResponseEnvelope
          from my_pa.bootstrap.settings import SettingsError, load_settings
          assert ResponseEnvelope.model_fields
          # MY_PA_DATABASE_URL is required and has no default, so the settings
          # smoke check supplies one. The URL is unreachable on purpose: this
          # proves the model builds at the floor, and nothing here connects.
          url = 'postgresql+psycopg://someone@db.invalid:5432/somewhere'
          assert load_settings({'MY_PA_DATABASE_URL': url}).redaction_enabled
          # And that the requirement itself survives the floor build, because a
          # validator that silently stopped raising here would be a fail-open.
          try:
              load_settings({})
          except SettingsError:
              pass
          else:
              raise AssertionError('an absent MY_PA_DATABASE_URL was accepted')
          print('contracts import and build at floor OK, version', my_pa.__version__)
          "
          # `mypy` with no path argument, deliberately: an explicit target
          # overrides `[tool.mypy] files` in `pyproject.toml`. Measured at
          # `bcdbf6d`: `mypy src` checked 93 files and a bare `mypy` checked
          # 109, so the 16 in `apps/` and `migrations/` were type-checked by
          # nothing in CI. Proven with a plant: a deliberate `-> str`
          # returning `int` in `apps/` left `mypy src` reporting "Success: no
          # issues found in 93 source files", and the same plant in
          # `migrations/env.py` did too, while a bare `mypy` caught both.
          # Both invocations are bare now, so they agree by construction
          # rather than by a number that has to be maintained. `D-64`.
          python -m mypy
          python -m pytest -m "not slow and not database and not network and not connector and not evaluation and not e2e and not recovery"

  database-current-head:
    # Ordinary current-schema tests. One Alembic upgrade per worker, then clones.
    runs-on: ubuntu-latest
    timeout-minutes: 60
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: my_pa
          POSTGRES_PASSWORD: ci_throwaway_not_a_secret
          POSTGRES_DB: my_pa_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready --username my_pa --dbname my_pa_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the package
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path
          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService
          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
      - name: Run current-head database tests
        shell: bash
        env:
          MY_PA_DATABASE_URL: postgresql+psycopg://my_pa@localhost:5432/my_pa_ci
          PGPASSWORD: ci_throwaway_not_a_secret
          PYTEST_PROVISIONING_REPORT: "1"
        run: >-
          python -m pytest -m
          "(database_clone or database_transactional) and not recovery and not e2e
          and not migration_edge and not migration_empty_to_head and not migration"

  database-recovery:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: my_pa
          POSTGRES_PASSWORD: ci_throwaway_not_a_secret
          POSTGRES_DB: my_pa_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready --username my_pa --dbname my_pa_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the package
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path
          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService
          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
      - name: Run recovery database tests
        shell: bash
        env:
          MY_PA_DATABASE_URL: postgresql+psycopg://my_pa@localhost:5432/my_pa_ci
          PGPASSWORD: ci_throwaway_not_a_secret
        run: python -m pytest -m recovery

  migration-empty-to-head:
    # Always on PR/push. Path filters must never drop this invariant.
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: my_pa
          POSTGRES_PASSWORD: ci_throwaway_not_a_secret
          POSTGRES_DB: my_pa_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready --username my_pa --dbname my_pa_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the package
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path
          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService
          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
      - name: Run empty-to-head migration invariant
        shell: bash
        env:
          MY_PA_DATABASE_URL: postgresql+psycopg://my_pa@localhost:5432/my_pa_ci
          PGPASSWORD: ci_throwaway_not_a_secret
        run: python -m pytest -m migration_empty_to_head

  migration-edge:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: my_pa
          POSTGRES_PASSWORD: ci_throwaway_not_a_secret
          POSTGRES_DB: my_pa_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready --username my_pa --dbname my_pa_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the package
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path
          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService
          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
      - name: Run migration-edge and loader tests
        shell: bash
        env:
          MY_PA_DATABASE_URL: postgresql+psycopg://my_pa@localhost:5432/my_pa_ci
          PGPASSWORD: ci_throwaway_not_a_secret
        run: python -m pytest -m "migration_edge or migration"

  database-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: my_pa
          POSTGRES_PASSWORD: ci_throwaway_not_a_secret
          POSTGRES_DB: my_pa_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready --username my_pa --dbname my_pa_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Record checkout identity
        shell: bash
        env:
          INTENDED_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          HEAD="$(git rev-parse HEAD)"
          TREE="$(git rev-parse 'HEAD^{tree}')"
          echo "repository=${GITHUB_REPOSITORY}"
          echo "checked_out_commit=${HEAD}"
          echo "checked_out_tree=${TREE}"
          echo "intended_commit=${INTENDED_COMMIT}"
          echo "github_sha=${GITHUB_SHA}"
          echo "github_ref=${GITHUB_REF:-}"
          echo "event_name=${GITHUB_EVENT_NAME}"
          git log -1 --format='subject=%s'
          if [[ "${HEAD}" != "${INTENDED_COMMIT}" ]]; then
            echo "::error::Checked-out HEAD is not the intended commit (${HEAD} != ${INTENDED_COMMIT})"
            exit 1
          fi
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install the package
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python - <<'PY'
          import inspect
          import tomllib
          from pathlib import Path
          from my_pa.adapters.mcp.server import _answer
          from my_pa.application.service import ApplicationService
          project = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          print(f"package_name={project['project']['name']}")
          print(f"package_version={project['project']['version']}")
          print(
              "invoke="
              f"{ApplicationService.invoke.__module__}.{ApplicationService.invoke.__qualname__}"
          )
          print(f"invoke_file={inspect.getfile(ApplicationService)}")
          print(f"answer={_answer.__module__}.{_answer.__qualname__}")
          print(f"answer_file={inspect.getfile(_answer)}")
          PY
      - name: Run logical e2e database tests
        shell: bash
        env:
          MY_PA_DATABASE_URL: postgresql+psycopg://my_pa@localhost:5432/my_pa_ci
          PGPASSWORD: ci_throwaway_not_a_secret
        run: python -m pytest -m e2e

  database-tier:
    # Required-check compatible aggregator. Does not check out or run pytest.
    # migration_historical has no tests on this tree and the repository has no
    # scheduled/release workflow appropriate to add one inside this change.
    # Historical compatibility remains a bounded follow-up, not a new job.
    if: always()
    needs:
      - database-current-head
      - database-recovery
      - migration-empty-to-head
      - migration-edge
      - database-e2e
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Reconcile database-tier lanes
        shell: bash
        env:
          CURRENT_HEAD: ${{ needs.database-current-head.result }}
          RECOVERY: ${{ needs.database-recovery.result }}
          EMPTY_TO_HEAD: ${{ needs.migration-empty-to-head.result }}
          EDGE: ${{ needs.migration-edge.result }}
          E2E: ${{ needs.database-e2e.result }}
        run: |
          set -euo pipefail
          echo "database-current-head=${CURRENT_HEAD}"
          echo "database-recovery=${RECOVERY}"
          echo "migration-empty-to-head=${EMPTY_TO_HEAD}"
          echo "migration-edge=${EDGE}"
          echo "database-e2e=${E2E}"
          for result in "${CURRENT_HEAD}" "${RECOVERY}" "${EMPTY_TO_HEAD}" "${EDGE}" "${E2E}"; do
            if [[ "${result}" != "success" ]]; then
              echo "::error::database-tier lane failed: ${result}"
              exit 1
            fi
          done

