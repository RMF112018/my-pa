# Python build and install artifacts
__pycache__/
*.py[cod]
build/
dist/
*.egg-info/

# Virtual environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# SwiftPM build products for the bounded WP-12 native source host package.
/native/apple-source-host/.build/

# Local configuration. Only .env.example is committed, and it holds no secret.
.env
.env.*
!.env.example

# Agent tooling: machine-local permissions and scratch worktrees, never shared.
.claude/settings.local.json
.claude/worktrees/
