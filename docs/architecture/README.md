# Architecture compatibility router

Current architecture starts at [`00_ARCHITECTURE_INDEX.md`](00_ARCHITECTURE_INDEX.md).

The former scaffold/navigation text at this path is superseded by the current-first architecture index. Retained architecture snapshots such as `system-context.md`, `module-boundaries.md`, and `data-authority.md` remain in Git history for traceability; current developer guidance is consolidated under the architecture index.
