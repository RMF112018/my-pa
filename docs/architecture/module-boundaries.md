# Retained historical architecture snapshot

**Status:** `SUPERSEDED_FOR_CURRENT_DEVELOPER_NAVIGATION`

The prior document at this path accumulated exact-head corrections and implementation chronology. Its previous content remains available in Git history.

Use [`backend-domain.md`](backend-domain.md) for the current implemented module boundaries and dependency direction. The executable enforcement remains in architecture tests such as `tests/architecture/test_dependency_direction.py`; current repository code and tests override historical prose.
