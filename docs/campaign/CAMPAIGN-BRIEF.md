# Campaign Brief — my-pa Completion Campaign

```yaml
campaign_id: MYPA-CANONICAL-APPLICATION-COMPLETION-PLAN-20260809-001
completion_plan_package: MYPA-CANONICAL-APPLICATION-COMPLETION-PLAN-20260809-001
canonical_product_package: MYPA-CANONICAL-PRODUCT-DEFINITION-20260809-009
validated_audit_package: MYPA-CURRENT-STATE-PACKAGE-20260809-001
repository: RMF112018/my-pa
remote: https://github.com/RMF112018/my-pa.git
operating_lineage: recovery/pre-20260805-utc-rollback-c9fb513
operating_lineage_head: 81589cc851905f9d63f5faf0690322682d1e8b85
operating_lineage_tree: 7eb87fa18f7a9f6718c6c9441dbecdf98453d543
reauthentication_date: "2026-08-09"
active_work_package: WP-14
active_work_package_name: Apple Tasks/To Do Feature-Package Extension Prerequisite
active_work_package_status: NOT STARTED — operator-gated. WP-14 is a specification prerequisite, not an implementation package; brief §9 forbids inventing the missing Apple Tasks domain contract, so it cannot begin until the operator supplies the approved feature-package extension.
wp_04_status: complete, reviewed, PR #57 open against the lineage, not merged
wp_05_status: complete, reviewed clean, PR open against the lineage, not merged; stacks on #57
wp_06_status: complete, reviewed clean, PR open against the lineage, not merged; stacks on #58
wp_07_status: complete, reviewed clean, PR #60 open against the lineage, not merged; stacks on #59
wp_08_status: complete, reviewed clean (zero blockers, six NOTEs), PR #61 open against the lineage, not merged; stacks on #60
wp_09_status: complete, reviewed clean (zero blockers, eight NOTEs), PR #62 open against the lineage, not merged; stacks on #61
wp_10_status: complete, reviewed clean (zero blockers, seven reviewer NOTEs), PR #63 open against the lineage, not merged; stacks on #62
wp_11_status: complete, reviewed clean (zero blockers, three reviewer NOTEs), PR #64 open against the lineage, not merged; stacks on #63
wp_12_status: complete, reviewed clean (zero blockers, nine reviewer NOTEs), PR #65 open against the lineage, not merged; stacks on #64
wp_13_status: complete, reviewed clean (zero blockers, nine reviewer NOTEs, eight closed by correction), PR #66 open against the lineage, not merged; stacks on #65
current_full_suite_baseline: "4615 passed / 1 failed / 0 errors — the time bomb is repaired and the 5 cascading errors are gone. The single remaining failure is the pre-existing, unowned web/node_modules architecture failure (WP-04 NOTE 6). Requires npm install to reproduce; a clean checkout reports 0 failed."
supersedes: WP-N01
completed_work_packages: [WP-01, WP-02, WP-S01, WP-03, WP-04, WP-05, WP-06, WP-07, WP-08, WP-09, WP-10, WP-11, WP-12, WP-13]
milestone_ms0: WP-01 -> WP-02 -> WP-S01 -> WP-03
```

**`operating_lineage_head` was stale and is corrected here.** It read
`60f8ccfba72cff3cd9be10164fca1f19af8d84e7`, which is the **parent** of the true
head: `git rev-parse recovery/pre-20260805-utc-rollback-c9fb513` and its
`origin/` counterpart both return `81589cc851905f9d63f5faf0690322682d1e8b85`
(WP-S01's own campaign-brief commit, which advanced the lineage past `60f8ccf`
without the figure being brought forward). `operating_lineage_tree` is corrected
with it. WP-03 was based on `81589cc…`, not on `60f8ccf…`.

**WP-03's integration is not recorded here.** The head and tree the operating
lineage reaches when `bf/wp-03-migration-chain-reconciliation` is merged are
recorded by the merging step; this frontmatter records the base it left from.
See [`WP-03-MIGRATION-CHAIN-RECORD.md`](WP-03-MIGRATION-CHAIN-RECORD.md).

This brief is the campaign's continuity aid, not a governance ledger. `AGENTS.md` remains the normative policy; this file states where the campaign currently stands against it.

## Supersession: WP-N01 -> WP-01

`WP-N01` was a local, improvised, pre-canonical work package. It was stopped mid-flight and produced **zero output and zero repository mutation**. It is superseded by canonical **WP-01 — Lineage, Goal-State, and Repository Current-State Correction Foundation**, defined by `MYPA-CANONICAL-APPLICATION-COMPLETION-PLAN-20260809-001`. Canonical WP-01 differs materially from WP-N01: it centers on documentation and goal-state correction and explicitly excludes branch move/merge, whereas WP-N01 had included repo-hygiene operations.

## Dependency status

- WP-01 has no dependencies.
- WP-01 -> WP-02 -> WP-03 forms milestone MS-0.
- Completed work packages: WP-01, WP-02, WP-S01, WP-03 (this line read "none yet" and was stale from WP-01's frontmatter onward).
- Remaining work: 34 bounded work packages across 8 workstreams per the completion plan.

## Open blocking findings

None. An independent review of WP-01 passed with no blocking findings; the two disclosed non-blocking gaps it raised are recorded below and are not blockers.

## Non-blocking follow-ups

- **Stale documentation outside WP-01 scope, pending follow-up:** `web/README.md` still declares `Status: IMPLEMENTING (WP-02/R1)` and lists "the Microsoft Graph connector (WP-07)" as pending delivery; `docs/architecture/system-context.md` cites a stale eleven-revision Alembic head (the actual head on the operating lineage is `d2e3f4a5b6c7` across 21 revision files). Both files were outside WP-01's declared in-scope path list and were deliberately left unchanged. They present pre-correction sequencing language to a reader, and `README.md` links to `web/README.md`. Route to a follow-up work package.
- **Archival note:** the superseded Moss v4.0 per-work-package merge ledger (merge SHAs and per-PR test counts) was not carried into this brief; it remains recoverable from `git log origin/main`.

## Specification prerequisites (open-decision register)

- `OD-COMP-001` — the supported native mechanism supplying Apple Tasks/To-Do is undefined; resolved in WP-14. Microsoft Graph must **not** be used as an implicit fallback. Blocks WP-19.
- `OD-COMP-002` — the current GoodNotes source path / runtime integration requires fresh investigation; the historical NAS-path package is not current authority. Resolved in WP-25. Blocks WP-26.
- `OD-COMP-004` — the offline encryption-key and signed-out/stale-auth policy must close before WP-08 acceptance. **RESOLVED — see D-17.** Closed under brief §12 (determinable from canonical sources, not an operator escalation under §13): plan doc 09 states a recommended default and the Manager adopted it — *device/browser-local protected key with explicit stale-session quarantine; document limitations*. **Reversible.**

## Source-of-truth direction

- **Apple-first personal-data ingestion**: Apple Mail, Apple Calendar, Apple Contacts, and Apple Tasks/To-Do, captured through the first-party native Apple architecture (`native/apple-source-host/`).
- **Microsoft Graph is retained but off by default**, and is **not** the active personal-data ingestion path. Entra authentication (used for the frontend's synthetic identity boundary) is a separate concern from Graph connector activation. A disabled Graph connector must never be reported as a degraded active source.

## Extreme-risk operator-only decisions

Reserved to the operator under `AGENTS.md` §8.2 (production activation, destructive/irreversible operations, credential mutation, live personal-data access, material risk acceptance, policy amendment, and the rest of that list). Not restated here.

## Invalidation rules

Any of the following invalidates this brief's identity binding and requires re-diff/review before continuing:

- a commit lands on the operating lineage (`recovery/pre-20260805-utc-rollback-c9fb513`);
- any source branch head listed in the topology table below moves;
- any of the three canonical packages named above changes.

## Operating lineage identity

- Repository: `RMF112018/my-pa` (`https://github.com/RMF112018/my-pa.git`)
- Operator-designated operating lineage: `recovery/pre-20260805-utc-rollback-c9fb513`
- Base head: `c9fb513a2afadf98f29b6d5ec3ad69db69e5ec1a`
- Tree: `9975318c731ac6150f251df7bdee5475c3b529d8`
- Reauthentication date: 2026-08-09 — exact match to the completion plan's planning-time claim; no drift.

## Branch topology (recomputed against recovery, 2026-08-09)

Format: "recovery-only / ref-only" = commits recovery is ahead / commits the ref is ahead.

| Ref | Head | Merge-base with recovery | recovery-only / ref-only |
|---|---|---|---|
| `origin/bf/wp-12e-frozen-baseline` | `6348b246f8be66f3db044d50a71b5d90aeeb3f57` | `88e8d81` | 7 / 2 |
| `origin/bf/mcv-neutral-remainder` | `86f13426fab91b4711c5557b10113800ea90a68a` | `634890e` | 11 / 7 |
| `origin/bf/extractions-quarantined-debt` | `688cc5047a06f6c193468568765378f23055250b` | `634890e` | 11 / 26 |
| `origin/dependabot/github_actions/actions/checkout-7.0.1` | `73bed38b1a6459789ff443c1882e49de9da88041` | `c9fb513` (= recovery) | 0 / 1 |
| `origin/main` | `6e491c24db97bd1ff2c537be4fbb58ff75ed2b81` | `634890e` | 11 / 2 |

Drift vs. the completion plan's planning-time claims: `bf/extractions-quarantined-debt` advanced from `f21e2f6000016a4c973f219d10a267b07e990c2a` (25 ahead) to `688cc5047a06f6c193468568765378f23055250b` (26 ahead). All other branch heads match their planning-time claims.

Local-vs-origin divergence in the primary checkout: local `main` and local `bf/wp-12e-frozen-baseline` both point at `88e8d8193095afa8d903db08324a588a5786908b` (an ancestor of recovery, 7 behind), differing from both `origin/main` and `origin/bf/wp-12e-frozen-baseline`.

Open PRs (2026-08-09, none draft): #44 `bf/wp-12e-frozen-baseline` -> `main`; #50 `dependabot/github_actions/actions/checkout-7.0.1` -> `main`; #53 `bf/mcv-neutral-remainder` -> `main`.

## Branch-reconciliation posture

Change-selective porting only. **No whole-branch merge** of `bf/wp-12e-frozen-baseline`, `bf/mcv-neutral-remainder`, or `bf/extractions-quarantined-debt` into the operating lineage. The dependabot branch (`dependabot/github_actions/actions/checkout-7.0.1`) is a simple candidate port: workflow-only, 1 commit directly ahead of recovery. Historical feature branches are preserved as history and are not remerged wholesale.

## Migration facts (bound to recovery head `c9fb513a…`)

21 revision files under `migrations/versions/`; exactly one Alembic head, `d2e3f4a5b6c7` (`20260805_d2e3f4a5b6c7_create_situation_frame_trace_project_tables.py`). No multi-head anomaly.

## Repository size (bound to recovery head `c9fb513a…`)

135 `.py` files under `src/`; 131 `.py` test files (149 files total) under `tests/`.

## Preservation record

The in-flight WP-12E slice-E work that was uncommitted in the primary checkout (26 entries) is preserved in commit `b2d597927a6b548830a1ed16340f8d19925496a1` on local branch `bf/wp-12e-slice-e-wip` (local only, not pushed). `bf/wp-12e-frozen-baseline` was left unchanged at `88e8d81`.

## Decisions

### D-01 — Operating lineage is recovery, not `main`

- **Decision:** the operating lineage is `recovery/pre-20260805-utc-rollback-c9fb513` at `c9fb513a2afadf98f29b6d5ec3ad69db69e5ec1a`, reauthenticated 2026-08-09.
- **Rationale:** GitHub's default branch is metadata, not operating-lineage authority; the operator designated recovery as the working lineage.
- **Evidence:** `c9fb513a2afadf98f29b6d5ec3ad69db69e5ec1a` — exact match to the completion plan's planning-time claim.
- **Invalidation:** a commit lands on `recovery/pre-20260805-utc-rollback-c9fb513` moving its head past `c9fb513a…`.

### D-02 — WP-12E slice-E work preserved, local-only

- **Decision:** the uncommitted WP-12E slice-E working-tree changes (26 entries) were committed to local branch `bf/wp-12e-slice-e-wip` at `b2d597927a6b548830a1ed16340f8d19925496a1` before WP-01 began; `bf/wp-12e-frozen-baseline` was left unchanged at `88e8d81`.
- **Rationale:** preserve in-flight work without altering the frozen-baseline branch. Kept local-only because `bf/wp-12e-slice-e-wip` diverges from its origin ref and pushing would require a force-push, which is prohibited under this work package.
- **Evidence:** commit `b2d597927a6b548830a1ed16340f8d19925496a1` on local `bf/wp-12e-slice-e-wip`.
- **Invalidation:** `bf/wp-12e-slice-e-wip` is pushed, merged, or its local head moves without a recorded reason.

### D-03 — Campaign brief location

- **Decision:** this brief lives at `docs/campaign/CAMPAIGN-BRIEF.md`, not the repository root.
- **Rationale:** canonical WP-01's in-scope path list is authoritative and does not include a repo-root brief.
- **Evidence:** WP-01 authorization's in-scope path list.
- **Invalidation:** a future canonical work package explicitly relocates it.

### D-04 — `bf/extractions-quarantined-debt` drift

- **Decision:** record the drift rather than silently re-deriving it.
- **Rationale:** `bf/extractions-quarantined-debt` advanced from the plan's `f21e2f6000016a4c973f219d10a267b07e990c2a` (25 ahead) to `688cc5047a06f6c193468568765378f23055250b` (26 ahead) between planning time and this reauthentication.
- **Evidence:** `origin/bf/extractions-quarantined-debt` at `688cc5047a06f6c193468568765378f23055250b`.
- **Invalidation:** WP-02 must re-diff against the current head before any porting decision from that branch.

## Operating rules in force

1. Repository governance (`AGENTS.md`) governs execution; the canonical completion plan governs product/roadmap intent; authenticated runtime and repository evidence outranks both when facts conflict.
2. No whole-branch merge of the three long-lived `bf/*` branches named above.
3. No force-push and no history rewrite. Branch/worktree deletion remains an operator decision.
4. Extreme-risk actions remain reserved to the operator (`AGENTS.md` §8.2).

## State update protocol

Update this brief's frontmatter and decision log at every material transition (work-package start, PR open, merge, blocker, invalidation). Superseded decisions move to history with their replacement identified — they are not silently rewritten.

### D-05 — WP-01 integrated into the operating lineage by fast-forward

- **Decision:** WP-01 was integrated into `recovery/pre-20260805-utc-rollback-c9fb513`, advancing it `c9fb513a… -> 49b6f034…` as a true fast-forward rather than by the squash that `CONTRIBUTING.md` sets as the default.
- **Rationale:** brief §34 requires the target lineage to advance per completed package, and leaving WP-01 unintegrated would force every later package to branch off a feature branch — the exact fragmentation this campaign exists to remove. Fast-forward was chosen over squash because squash mints a SHA that neither of WP-01's two independent exact-head reviews examined; fast-forward preserves the reviewed identity `49b6f034…` byte for byte.
- **Evidence:** push reported `c9fb513..49b6f03` (two-dot, non-forced); resulting tree `6bd5da8292acfa62a503f90bb6b7e31217226b9f` equals the reviewed WP-01 tree. PR #54 was opened as the repository-native record and GitHub marked it merged from the fast-forward. Architecture suite 1342 passed at the new head.
- **Invalidation:** none outstanding.

### D-06 — WP-02 squash-merged, deliberately, for a security reason

- **Decision:** WP-02 was squash-merged into the operating lineage rather than merged with a merge commit, and no pull request was opened for `bf/wp-02-selective-branch-reconciliation`.
- **Rationale:** an intermediate commit on that branch carried reproduction detail for a then-unpatched defect. Squashing kept it from becoming a permanent ancestor of this public lineage; opening a pull request would have minted a permanent public `refs/pull/N/head` carrying the same blob. The merged tree `d1bc78bc…` is byte-identical to the independently reviewed tree, so the review carries over on content despite the new SHA.
- **Evidence:** the commit is not an ancestor of the operating lineage; merged tree equals reviewed tree `d1bc78bc073f2980fa547507e996b94156e5e75e`. The branch `bf/wp-02-selective-branch-reconciliation` has since been deleted from the remote under operator authorization, so nothing *at head* advertises the commit. That is the whole of the claim: an earlier wording here said "nothing in this repository," which was false and contradicted the residual immediately below it. The parent commit `c3711a1…` still carries the identifier verbatim in a tracked blob, and `git log -S` over the history surfaces it in one command. Removing it from history would be a rewrite, and this campaign has declined to perform one.
- **Residual, stated plainly:** deleting the branch and omitting the SHA removes *forward signposting only*. It is not a history rewrite and the commit is not gone: it remains in GitHub's object cache and is retrievable by anyone who already holds or can guess its identifier, until GitHub Support purges it. No claim is made here that history was scrubbed or purged.
- **Invalidation:** superseded once GitHub Support confirms the purge, after which the ordinary pull-request route resumes. The defect the commit concerned is itself now patched (see the backlog item below), which lowers the value of the residual but does not remove it.

### D-07 — WP-02 ported 8 of 29 ahead-commits; the rest are declined or deferred with reasons

- **Decision:** of 29 unique ahead-commits across the four retained branches, 8 were ported, 6 declined as direction-incompatible or false-at-target, and 15 deferred to WP-03. No branch was merged wholesale and no migration file was touched.
- **Rationale:** `bf/extractions-quarantined-debt` strictly contains `bf/mcv-neutral-remainder` (identical SHAs), so the true unique population is 29, not the 36 a naive per-branch sum suggests. Every commit is accounted for exactly once.
- **Evidence:** `docs/campaign/WP-02-INTEGRATION-RECORD.md`; independently recomputed by the reviewer as 8 + 6 + 15 = 29 with zero unaccounted and zero phantom entries.
- **Invalidation:** any of the four source branch heads moves.

### D-08 — WP-S01 was inserted ahead of WP-03 by operator decision, as its own bounded package

- **Decision:** the URL-parser divergence carried out of WP-02 as backlog item 1 was fixed as its own work package, **WP-S01**, executed before WP-03 rather than deferred. The operator was presented the verified facts and chose to remove the underlying defect rather than only its disclosure, and to do so immediately as a bounded package.
- **Rationale:** the defect was live at head and the branch carrying its reproduction detail was still published. Containing the disclosure without patching the defect would have left the defect; patching without containing would have left the disclosure. Both were closed in one cycle.
- **Scope executed:** the remote branch `bf/wp-02-selective-branch-reconciliation` was deleted under explicit, narrow operator authorization — an exception to the standing prohibition on branch deletion, covering that one remote ref and nothing else. Its local counterpart was removed afterwards as an ordinary bounded work-branch cleanup under `AGENTS.md` §8.1, once its content was re-derived as fully contained in this lineage.
- **Evidence:** before deletion the branch tip differed from this lineage in exactly one file, `docs/campaign/CAMPAIGN-BRIEF.md`, and on the stale side only — every other path was byte-identical, so nothing unique was lost. Merged tree `8ccdc862e90d61858b540b3a40e881f368303269` equals the independently reviewed tree.
- **Residual:** deleting the branch removed forward signposting; it is **not** a history rewrite. See D-06.
- **Invalidation:** none outstanding.

### D-09 — native-plane principal partitioning deferred to WP-04

- **Decision:** WP-03 adds no principal scoping to the native-source plane. Deferred to **WP-04**.
- **Rationale, and it is a measured premise rather than a judgement call:** WP-02 carried forward an acceptance condition that WP-12E's "baseline tables must be principal-partitioned before admission". Revision `a7c3e8d1f642` contains **zero `CREATE TABLE`** — it creates no tables. The four it alters are created unpartitioned by `8c4d1e7a2b90` and `9d5e2f7b4c61`, both already on the chain. All **22** `native_*` / `source_*` tables in `knowledge` are unpartitioned and carry no `principal_id`, so partitioning a subset would leave partitioned children joined to unpartitioned parents. The condition is moot in any case: the revision was not admitted (see D-10).
- **What the acceptance clause does ask — "principal constraints/indexes preserved" — was verified, not assumed:** 30 `principal_id_is_an_opaque_identifier` CHECKs, 39 `by_principal` indexes, 1 `a_capture_key_admits_one_submission_per_principal`, identical **by name set** across `d2e3f4a5b6c7`, the new head `9d4e7a3b1c62`, and a full round trip, all read from `pg_catalog` on live PostgreSQL 17.10.
- **Evidence:** `docs/campaign/WP-03-MIGRATION-CHAIN-RECORD.md`; the superseded condition is annotated in place in `docs/campaign/WP-02-INTEGRATION-RECORD.md`.
- **Invalidation:** any revision that adds `principal_id` to, or partitions, a `native_*` / `source_*` table before WP-04.

### D-10 — WP-03 re-authored 12 of the 15 deferred commits; 3 deferred with reasons

- **Decision:** of the 15 commits WP-02 deferred to WP-03, **12 were re-authored** as five items of work, and **3 are deferred again**, each with a named owner or a named reason.
- **Rationale:** 14 of the 15 conflict under 3-way merge against this lineage, so the source commits were used as specifications rather than cherry-picks. The three not carried: `355a0f8b` / `6348b246` (revision `a7c3e8d1f642`) — **attempted and reverted**, DDL-coherent but application-incompatible, blocked on WP-12E's application half (~707 lines of `native_sources.py`, a new `application/native_baseline.py`, contract widening), which is feature behavior and not migration-chain work; `288bdb14` / `dcc97266` — superseded **within their own source branch**, which deletes the file they harden; `6e491c24` — out of scope, code-inert here, already on `origin/main`.
- **Evidence:** `docs/campaign/WP-03-MIGRATION-CHAIN-RECORD.md`, which names the source commits per re-authored item and records the reverted attempt at 8 failed + 4 errors in `tests/schema`, independently reproduced.
- **Invalidation:** any of the four WP-02 source branch heads moves.

## Triaged backlog carried out of WP-02

These are recorded decisions, not oversights. None blocks WP-03.

1. **URL-parser divergence — FIXED by WP-S01; no longer live.** `src/my_pa/bootstrap/settings.py` validated the database URL with one parser while `src/my_pa/infrastructure/database/engine.py` let the engine parse it with another, so the configuration the validator approved was not necessarily the one the engine connected to. It is now parsed exactly once, by the parser that governs the connection, and that same parse is what the engine is configured with — there is no second reading left to diverge from the first. Regression coverage is of two kinds, and saying so is the point: an earlier version of this sentence named only the first and was measured to guard **one of seven** production call sites. `tests/unit/test_settings.py` counts the parser and asserts one reading of the string per `load_settings`, and `tests/unit/test_gateway_composition.py` asserts the object validation approved is what the gateway's engines are configured with — behavioural, and between them they cover `bootstrap/gateway.py` alone. The other six callers are held structurally by `tests/architecture/test_connections_open_on_the_single_validated_parse.py`, which parses every production module's syntax tree and rejects any `create_database_engine` call whose URL argument does not resolve — through local bindings, so an aliased parse still counts — to `Settings.parsed_database_url()`. All seven sites fail that guard when reverted to `.database_url`; six of them reddened nothing before it existed. What the guard does **not** do is behavioural: it shows that no production module *asks* for a second parse, not that the engine received the approved object. That half remains the two unit tests', and it remains gateway-only. All of it is expressed as invariants rather than as inputs. This was carried out of WP-02 as its own bounded work package, which is what WP-S01 was. Reproduction detail remains withheld from this public repository under `SECURITY.md`.
2. **The conditional split of `6e491c24…` was declined** as not cleanly separable — its `settings.py` hunk and all five of its tests are welded to statement-timeout machinery WP-02 was not authorized to port. 21 files of that commit are unported and enumerated in the integration record.
3. **`docs/plans/mcv-completion-plan.md` is deliberately untouched** beyond a single re-derived count. It still presents the superseded MCV work-package order as current while remaining linked from `README.md`. This is a documentation-reconciliation item and is **not** WP-03 scope (WP-03 is the migration chain only).
4. **WP-03 acceptance condition:** WP-12E's baseline tables were authored before principal partitioning existed and carry no `principal_id`. Any re-authoring of those migrations into the single chain must address this. — **Superseded by D-09, on a measured premise: there are no such tables.** Revision `a7c3e8d1f642` contains zero `CREATE TABLE`; the four tables it alters were created unpartitioned by revisions already on the chain, and all 22 `native_*`/`source_*` tables in `knowledge` are in that same state. Moot regardless, because the revision was not admitted (D-10). Original wording left standing above.
5. **The database tier has now been executed in this environment — this item is withdrawn as written.** It previously read "has never been executed in this environment (no PostgreSQL reachable)," and that is no longer true. PostgreSQL 17.10 runs in a local Docker container and the full suite has been observed to pass against it end to end, twice and by two parties: at **3639 passed, zero failures and zero errors** by an independent second context that ran it itself before this correction, and at **3657 passed, zero failures and zero errors** at head `02b1f4e` by an independent reviewer who collected and ran it there, which is the same suite plus the sixteen tests this correction adds and the two further unit tests that landed after it. This sentence read **3655** when it was written, which was true at `f202fb6` and went stale by two the moment those unit tests landed without the figure being brought forward; the number here is now the head one, and it is the head, not the correction alone, that the count belongs to. An earlier run reporting `3139 passed, 500 errors` is superseded and was not evidence of defects: every one of those errors was `password authentication failed`, from supplying a password to a server configured for `trust` on loopback — a misconfiguration of the run, corrected before the figure above. The consequence for WP-02's `search.py` redaction fix is that it is no longer covered by the AST guard alone; its two end-to-end tests are inside that suite and ran. What is still **not** claimed: no continuous-integration environment has been shown to reproduce this, the container is a developer's local one rather than a provisioned tier, and WP-03 still needs a live PostgreSQL of its own to discharge its acceptance criteria. **WP-03 has since discharged them against its own PostgreSQL 17.10 container** — empty→head, prior-head→head, and a full round trip, with `tests/schema` at 223 passed and the whole suite at **3660 passed, 0 failed, 0 errors**. The CI half of this item stands: no continuous-integration environment has yet been shown to reproduce it.

## Backlog carried out of WP-03

Neither blocks the merge of WP-03, and neither is WP-04 scope by default — both need an owning package named.

6. **The `render_as_string(hide_password=False)` sites across the test tree — deferred a second time, and it now needs an owner.** WP-02 raised this as a redaction note *for WP-03*; WP-03 closed the one site it had authored that did not need to exist (review finding F1) and was not authorized to sweep the rest. Measured at WP-03's head: **72 sites across 36 test files**, all under `tests/`, none in production code. **35** are the maintenance-engine shape and are avoidable by the same one-line change F1 made — `create_database_engine` takes a `URL`, so the render is pure loss. **36** write a rendered DSN into `MY_PA_DATABASE_URL` because `migrations/env.py` reads the URL from the environment at import time; those are structural and cannot be closed without changing how Alembic is handed a connection here. **1** renders a deliberately closed port for a negative probe. Nothing logs any of them, but a rendered traceback over the local could disclose a database password. Schedule the two families together, so the structural one is not taken as the excuse for the avoidable one.

7. **The `NativeRunState.RUNNING` widening hazard — must be resolved when WP-12E is rescheduled.** `tables.py` derives **two** database CHECKs from the one `NativeRunState` enum: `native_run_state_is_known` on `knowledge.native_sync_runs` and `native_bucket_run_state_is_known` on `knowledge.native_bucket_runs`. Revision `a7c3e8d1f642` widens only the first; it does not alter `native_bucket_runs` at all. So adding `RUNNING` to the enum desyncs the second site, and **no test catches it** — the parity guard compares constraint *names* (`pg_constraint.conname`), not `pg_get_constraintdef` text, so a constraint that keeps its name and changes its vocabulary is invisible to it. Whoever reschedules WP-12E owns both the second `ALTER` and a guard that compares definitions rather than names.

### D-11 — WP-04 proved isolation on the planes that gate the rest, and named the rest as residual

- **Decision:** WP-04 closed Principal isolation on **identity derivation, durable persistence (relationship plane), jobs/queues, review and capture**, and proved foreign-vs-absent indistinguishability on those planes by result *and* error class. **Search is proved only at the authorization gate**; the **native-source plane is not isolated** and is quarantined; the **web BFF** has a proved fail-closed session secret but no two-Principal test, because it holds no durable state and reaches no backend at this head.
- **Rationale:** the eight in-scope planes exceeded one package. Identity derivation and durable persistence gate everything else, so they were proved properly rather than all eight proved thinly. Coverage is stated as achieved, never implied.
- **Evidence:** head `a565c2b`, PR #57. Full suite including the PostgreSQL 17.10 tier: 4117 passed, 0 errors. Architecture suite 1822 passed. Single Alembic head `4f1a8b6d92e3`.
- **Invalidation:** any change to identity derivation, authorization, or user-scoped persistence invalidates the isolation review (canonical WP-04 invalidation rule).

### D-12 — the inherited "WP-12E baseline tables carry no `principal_id`" claim is refuted as worded

- **Decision:** record the correction rather than restate the inherited framing. Revision `a7c3e8d1f642` is **not on this chain** and there are **no WP-12E baseline tables at this head**, so the claim carried out of WP-02 as acceptance condition 4 is false as written.
- **Rationale:** the real analogue is larger, not absent. Revision `1e6c0a94f3b7` creates a **484-table** migration target across seven schemas carrying **zero** principal scoping — no `principal_id`, no owner column. No application module reads those schemas, so isolation there is **vacuous today**, but it is the destination for real user data and has no column to scope on.
- **Evidence:** measured at head — `grep -c "CREATE TABLE"` on the target SQL = 484; `principal_id` = 0. A guard asserts no module names those seven schemas.
- **Invalidation:** any application read path into `core`, `procore`, `financial`, `schedule`, `email`, `construction` or `calendar` makes this immediately release-blocking under brief §18.

### D-13 — WP-05 wired Entra authentication into the real request path, operator-gated and fail-closed

- **Decision:** `PrincipalIdentityService` — which WP-04 handed over with **zero production call sites** — now has one, and it is the actual HTTP request path. `MY_PA_AUTH_MODE` selects between `local_operator` (the default, byte-identical to the `D-30` loopback behaviour this build has always had) and `entra`, which requires `Authorization: Bearer <token>`, verifies it, and derives the Principal from the token's validated `tid`/`oid`. Activation is a configuration change and nothing else; the four `MY_PA_ENTRA_*` values are blank in `.env.example` and are the operator's to supply.
- **Fail-closed, with no downgrade:** `entra` mode missing any of the four values **fails startup** rather than falling back to `local_operator`. `create_http_app` refuses both-or-neither. MCP-over-stdio and the CLI refuse (exit 2) in `entra` mode rather than serving as the local operator. On the web side `MYPA_AUTH_MODE` has no default and no `||` fallback; unset is a 500 refusal, synthetic sign-in is refused in production and refused (403) in `entra` mode. WP-04's `MYPA_SESSION_SECRET` control is unregressed and now carries an explicit regression test.
- **The sharp edge, stated rather than buried:** `contracts.v1.RequestMetadata` makes `principal_id` a **required envelope field on every public request**, so `reject_caller_supplied_principal` runs on the payload rather than the whole document. The stronger property is what holds instead, and it was established by trace and by plant rather than by docstring: `metadata.principal_id` is read **nowhere** in production code — not in authorization, persistence scope, query filters, audit, or read models. Every consumer reads `authorization.principal.principal_id`. Planting `principal_id=metadata.principal_id` into `service._run` turns both a behavioural test and an architecture guard red.
- **`PrincipalKind.OPERATOR`** was chosen deliberately: it is the kind `local_principal()` already issued, so turning authentication on changes *which identifier acts* and nothing about *what it may do*. `GATEWAY` would have silently narrowed authority as an unreviewed side effect of an authentication change.
- **Dependency:** `PyJWT>=2.10,<3` with the crypto extra, declared and confined by architecture test to `infrastructure/security`. The standard library has no JWS implementation, and a maintained library avoids the alg-confusion class of bug. Algorithms are pinned to `RS256` as a module constant and are never read from the token header; `aud`/`iss`/`exp`/`nbf` are all verified.
- **No Graph dependency for sign-in:** `msal.config.ts` requested the Graph scope `User.Read`; it is gone. Sign-in requests `openid`/`profile`/`offline_access` only, a configured API scope matching a Graph marker is dropped, and a guard scans the sign-in path for Graph machinery and the tree for a Graph SDK. Nothing reports a disabled Graph connector as a degraded active source.
- **Session lifecycle:** signed session gains a random `sid` and `iat`; sign-in rotates the `sid` and revokes the principal's prior one (no fixation); sign-out **revokes server-side**, proved by replaying the exact same cookie value against a real route and getting 401 rather than by checking a cleared cookie.
- **Evidence:** head `9c3347b`. Full suite **4159 passed, 1 failed** (the pre-existing `node_modules` architecture failure, NOTE 6 below) against the base head's 4117/1; architecture 1831 + that same 1; web **88 tests passed** against a baseline of 49; ruff/format clean, mypy clean over 166 source files. Single Alembic head `4f1a8b6d92e3`, unchanged — no migration. Independently reviewed at the exact head: **clean, zero blockers**, with eleven controlled reversions all observed red for the intended reason.
- **Invalidation:** canonical WP-05's own rule — any auth library, configuration, claims, or session change requires re-review.

## NOTEs carried out of WP-05 (§15A — recorded, not blocking)

1. **`resolve_or_create` unconditionally re-activates a suspended account.** `infrastructure/persistence/user_accounts.py` sets `lifecycle_state = ACTIVE` in its `ON CONFLICT DO UPDATE` arm, and the authenticator inspects neither `lifecycle_state` nor `consent_state`. A locally-suspended account presenting a valid token would be silently reactivated. Pre-existing WP-04 code, **unreachable at this head** because nothing in the tree writes `SUSPENDED` or `DEPROVISIONED` — it becomes real the moment an offboarding path is written, and whoever writes one owns this.
2. **No audit event for an authentication refusal.** In `entra` mode a 401 returns before any unit of work opens, so a rejected token, foreign tenant, or smuggled-identity payload leaves no audit record. There is no resolved Principal to attribute it to, and `AGENTS.md` §5 asks this of denied *destructive* attempts, which these are not. Observability gap.
3. **"Never call `verifySession` from Node" is a convention, not a guard.** Every current call site obeys it — all 10 API routes and all server components resolve through the revocation-checking path, and the only remaining `verifySession` caller is Edge middleware — but no test would catch a *new* route that called it directly. The Python tree has an architecture test for exactly this drift class; the web tree does not.
4. **Web revocation is process-local and lost on restart, and Edge middleware cannot enforce it.** The web tier has no durable store at this head. Middleware keeps a signature+expiry pre-filter and says so in its own source rather than pretending to be the authority. WP-06 wires the backend and is the natural owner.
5. **The planted-root architecture control is fractionally looser for five roots.** It now asserts "prohibited **or** confined" rather than "prohibited", which is the price of covering `jwt` at all. Confinement is separately enforced per-module, so nothing escapes package-wide.
6. **Two `render_as_string(hide_password=False)` sites were added** in the new test file, joining the 72 already recorded in backlog item 6. Same family, same deferred owner.

## NOTEs carried out of WP-04 (§15A — recorded, not blocking)

1. **Native-source plane is unpartitioned.** 22 tables plus the Swift spool have no principal column at either layer, and `native_sources.py` uses a **global** advisory-lock namespace, so one Principal's operation can block another's. Quarantined in an exact registry that fails the build if it changes. Needs its own package — it is the one unpartitioned plane that *is* wired to the application.
2. **`persistence/search.py` holds no principal predicate.** Scoped only by `_scope_of_enrollment` resolving enrollment ids within the caller's own enrollments. A test now asserts that single point; the query itself has no defence if a future caller bypasses the gate.
3. **`relationship_identity_observations` unique constraint is table-wide.** Two Principals recording the same source version collide with `IntegrityError` where an absent row succeeds — an existence-disclosure channel. Predates WP-04, unreachable today (no transport wiring). **Becomes a blocker the moment the relationship plane is wired.**
4. **Neither transport authenticates a real Entra token.** The Python gateway uses one fixed process principal; the web BFF uses two hardcoded synthetic principals. `PrincipalIdentityService` — the only code turning validated claims into a Principal — has zero production call sites. No test exercises two Principals *through a transport*, because no transport can carry two. This is WP-05's subject and was deliberately out of WP-04 scope. — **CLOSED by WP-05 (D-13).** The Python HTTP transport now authenticates a real token in `entra` mode and derives the Principal from validated `tid`/`oid`; `PrincipalIdentityService` has a production call site; and two Principals now resolve to two identities *through the transport*, neither reaching the other's state. The web BFF's synthetic provider is retained but explicitly mode-gated and refused in production.
5. **Guard limits, recorded in the guards' own comments.** `except ... as` and `match ... case` captures are unchecked binding routes; an accessor bound to a local alias splits a read across two statements neither detector joins; annotation-grounded trust is defeatable by `Any` or a module-local alias of a trusted type name. None is exercised in this tree. The guards state what they measure, not an impossibility.
6. **`test_every_python_root_is_type_checked_or_named` fails whenever `web/node_modules` is installed.** It excludes only root `.gitignore` patterns while `node_modules` is listed in `web/.gitignore`, so one vendored `.py` file makes `web` look like an unchecked Python root. Pre-existing, CI-invisible (no workflow installs web dependencies). Fix by reading nested `.gitignore` files or excluding `node_modules` by name.
7. **Queue partitioning is enqueue/claim/reap/status; `principal_bound_values` gained a `table` parameter.** `claim_job`, `reap_abandoned_jobs` and `run_worker` now take a **required** `principal_id` with no default, and `web` refuses to serve without `MYPA_SESSION_SECRET`. These are intentional fail-closed breaks, not regressions.

### D-14 — WP-06 wired four of the seven named surfaces to the real backend; the other three fail closed rather than serving fixtures

- **Decision:** the Next.js BFF now speaks the canonical `POST /v1/{capability}` envelope to the Python gateway through a new server-only transport (`web/src/lib/api/gateway.ts`). **System** (`capabilities.get`), **Library** (`capture.list` / `capture.search` / `knowledge.search` / `knowledge.read`), **Review** (`review.list`, and decide via `review.decide`) and **Capture** (`capture.create`) are real-backed end to end. **Today/Pulse, Situations and Reveal are not**, and they return `501 not_implemented` in a default build rather than a fixture. — **Superseded: all three are now real-backed.** Reveal was wired by WP-09 (D-18); **Today/Pulse, Situations and Projects were wired by WP-11 (D-20)**, which added the three capability seats whose absence is the deferral reason recorded in the next sentence. The sentence is left standing because the *reason* it gives remains exactly right and was the seam WP-11 executed against.
- **The deferral reason is measured, not asserted, and was independently confirmed:** `Capability` has exactly 15 members and revision `3c8f1e2a5b74` freezes `audit_events.capability_is_known` to those 15 plus 11 `native_sources.*` names — 26, read out of live PostgreSQL 17.10 at head. `authorize()` writes an audit row inside the transaction on **every** invocation, so the CHECK genuinely bites. A 16th public capability therefore requires a forward `ALTER`, i.e. a migration, which was explicitly out of WP-06 scope. `SqlPulseRepository` and `SqlSituationRepository` are real and principal-scoped; only the capability seat is missing. **Reveal is a different absence:** no capability takes a subject id and returns evidence spans / derivation trace at all. Routing Reveal at `knowledge.read` would have answered a different question and been faking.
- **Synthetic is gated on `MYPA_DATA_PROVIDER`** — unset by default, no `||`/`??` fallback, any non-`synthetic` value refused, and refused outright when `NODE_ENV=production`. The refusal lives in `web/src/lib/fixtures/gate.ts` and **every fixture entry point calls it**, including `syntheticDisclosure`. Route-level gating alone would have left four server components serving fixtures with every route test green.
- **No Principal is supplied by the browser.** A foreign `principal_id` in body, nested body, query string, `x-principal-id` or `x-ms-client-principal-id` reaches the wire nowhere; the envelope's `principal_id` is session-derived. WP-05's property (D-13) is preserved: `metadata.principal_id` is still read nowhere in production code. Planting `principal=replace(principal, principal_id=metadata.principal_id)` in `service.py::_run` reddens two architecture tests and four behavioural ones.
- **`acknowledged_not_persisted` is gone from both backend paths.** `capture.create` returns a durable receipt (`receipt_id`, `capture_id`, `version_id`, `version_number`, `idempotency_key`, `content_sha256`, `issued_at`, `created`) written with the capture, version, submission and queued job in one transaction; idempotency is PostgreSQL's `UNIQUE (principal_id, idempotency_key)`. The literal survives only on the explicitly-enabled synthetic path, where it is still exactly true.
- **Evidence:** head `e8bc93d`. Full suite **4171 passed, 1 failed** against the base's 4159/1; architecture **1831 passed** + that same 1; web **140 passed** against a baseline of 88; ruff/format clean over 549 files, mypy clean over 166. Single Alembic head `4f1a8b6d92e3`, unchanged — no migration. Independently reviewed at the exact head: **clean, zero blockers**, with all figures re-derived by the reviewer on its own PostgreSQL 17.10 container and matching exactly, and four controlled reversions re-planted and observed red for the intended reason.
- **Invalidation:** canonical WP-06's own rule — contract/schema changes invalidate the BFF parity tests.

### D-15 — `local_operator` is not session-partitioned; this is disclosed, not prevented, and it gates WP-07

- **Decision:** recorded as a NOTE rather than a blocker, on the independent reviewer's evidence-based judgment, with a named discharge condition below. This is the sharpest honesty edge in WP-06 and is recorded in full rather than softened.
- **The exposure is live, not theoretical, and was demonstrated rather than argued.** The reviewer stood the whole chain up — its own PostgreSQL, `apps/gateway.py` in `local_operator`, `next dev` with `MYPA_DATA_PROVIDER` unset — signed in as `synthetic-a`, captured a note, then signed in as `synthetic-b` and **read A's capture back through `/api/library`**, including a full-text match on A's exact note text. The Python gateway serves one fixed process principal regardless of which web session is signed in. Nothing *prevents* cross-principal service; `LOCAL_OPERATOR_LIMITATION` is present on every backend-served response and *discloses* it.
- **Why it does not block at this head:** no configuration here can produce two *real* Principals. The only multi-identity path is the synthetic development provider, which `web/src/lib/auth/mode.ts:74` refuses when `NODE_ENV=production`; the only real-identity path is `entra`, which the BFF refuses (503 `no_forwardable_credential`, verified live, `fetch` never called). A production build therefore serves no backend data to anyone. The two "Principals" are development fixtures belonging to the one human running the machine, reading that human's own single dataset. `AGENTS.md` §3 defers multi-user operation during MCV.
- **The framing was binary and did not need to be.** The implementer presented serve-with-disclosure vs. refuse-everything. The reviewer identified a third option that was not recorded as considered: refuse backend data when `MYPA_GATEWAY_AUTH_MODE=local_operator` **and** the web tier can admit more than one Principal, or pin the synthetic provider to a single principal in that combination. One principal + `local_operator` still runs; the two-costume path disappears.
- **Discharge condition — this becomes release-blocking under brief §18 the moment either holds:** a second real Principal can sign in, or `local_operator` ships paired with any multi-identity web auth mode. **WP-07 writes durable Quick Capture records and must discharge this first**, because it is the package that makes the cross-principal read carry real user data.
- **Discharged in WP-07, by the reviewer's third option.** The web tier now admits **exactly one** Principal whenever `MYPA_GATEWAY_AUTH_MODE=local_operator`: the admissible set itself narrows to `synthetic-a`, deterministically and with no new environment variable. The narrowing lives in `web/src/lib/auth/synthetic.ts` — the full catalogue is module-private and `admissibleSyntheticPrincipals()` is the only listing anything can import, so `POST /api/session`, the sign-in screen and any later consumer get the narrowed set without having to call a gate. That placement is WP-06's own `fixtures/gate.ts` lesson applied: a gate that has to be *called* is a gate that will be missed. `POST /api/session` refuses the non-pinned key with a typed `principal_not_admissible` (403) rather than rebinding the caller to the pinned principal, and `/sign-in` became a server component so it offers one button instead of one that is guaranteed to fail. `entra` is untouched — two real Principals there are two real datasets — and an unconfigured gateway mode is untouched because no backend request is made at all in that state. `LOCAL_OPERATOR_LIMITATION` stays: prevention plus disclosure beats either. Evidence: `web/src/lib/auth/admissible-principals.test.ts` (`synthetic-b` cannot sign in at all under `local_operator`; both remain admissible under `entra`) and `web/src/app/sign-in/sign-in.test.tsx`. Reverting the pin turns them red with `expected 200 to be 403` and a live session cookie issued for `synthetic-b`.

## NOTEs carried out of WP-06 (§15A — recorded, not blocking)

1. **`local_operator` cross-principal service** — see D-15. Carried a named discharge condition and was WP-07's entry gate. **Discharged in WP-07**: the admissible Principal set narrows to one under `local_operator`, so the second identity does not exist to read the first's captures with.
2. **Acceptance clause 1 is only partly satisfied: 3 of 7 named surfaces are not backend-backed.** Whether "use real backend contracts" means "reach a real read model" or "stop serving synthetic and speak the real contract honestly" is genuinely ambiguous; ambiguous defaults to NOTE under §15A, and the deferral reason (migration boundary) was independently verified true. **Stated as achieved, never implied.**
3. **The contract-parity call-site scan covers 4 of 8 declared capabilities.** `test_no_web_module_addresses_a_capability_the_contract_does_not_declare` matches `callGateway(x, "literal")`; `/api/library` passes a **variable**, so `knowledge.read`, `knowledge.search`, `capture.search` and `capture.list` escape it. The TS type still narrows the argument. The docstring calls itself "the second lock rather than the first" but does not disclose this specific gap.
4. **The newly real-backed routes have no UI consumer.** `library/page.tsx` is still a `NotConnected` placeholder; `/system` renders from the session, not `/api/system`. So `LOCAL_OPERATOR_LIMITATION` is on every backend response but is rendered to a human nowhere. `web/README.md` discloses the four fixture-reading pages were not rewired; it does not say the wired routes have no reader.
5. **Server components fail closed but fail ugly.** `today`, `review`, `situations` and `relationships/[personId]` call gated fixtures unconditionally and there is no `error.tsx`/`global-error.tsx`, so a default build yields an unhandled 500 rather than the honest typed refusal the routes give. No fixture data renders — the property holds — but the failure shape is wrong. — **Partly closed by WP-11:** `today` and `situations` now render real backend data on the default path and a typed refusal rather than an unhandled 500. `review` and `relationships/[personId]` are unchanged and the NOTE stands for them.
6. **`schemaHead` staleness understated.** `web/src/app/api/system/route.ts:13` says the removed `e7f3a9c2d514` was stale "by three revisions"; it is five behind head. The removal itself is honest — the field is gone, a test asserts it, nothing replaced it with a different claim.
7. **`.env.local` shadowing.** Shell env wins over `.env*` in Next, so a shell-set `MYPA_GATEWAY_AUTH_MODE` silently overrides `.env.local`. Not a defect of this change; it can make an operator believe a mode change took effect when it did not.
8. **`entra` credential forwarding is unimplemented and the BFF refuses rather than degrading.** The session envelope carries no bearer token and `POST /api/session` implements no real sign-in. Carrying a token in the HMAC-signed-but-**readable** cookie would disclose it. Needs an Entra app registration and an MSAL path — operator-gated.
9. **`test_every_python_root_is_type_checked_or_named` still fails** whenever `web/node_modules` is installed (WP-04 NOTE 6). Unchanged, untouched, not weakened; it is the single failure in both the base and this head.

### D-16 — WP-07 discharged D-15 and proved the four durable-first controls WP-06 had left unproved

- **Decision:** WP-07 closed **D-15** before touching durable capture, using the WP-06 reviewer's third option rather than the implementer's binary. The full narrative is appended to D-15 above. The essential property: under `MYPA_GATEWAY_AUTH_MODE=local_operator` the web tier admits **exactly one** Principal, so the second identity that read the first's capture in WP-06 no longer exists to read it with. `entra` is untouched, because two real Principals there are two real datasets.
- **The diff touches no `src/` and no `migrations/`.** It is 2 docs files, 5 `tests/capture/` modules and 13 `web/` files. The Python durable-first behaviour was already built by WP-06; WP-07 **proves** it and wires the web surface to it. No new capability, no migration — the 15-member `Capability` seat and its frozen `capability_is_known` CHECK are untouched, and the reviewer independently confirmed a 16th genuinely requires a forward `ALTER`.
- **Acknowledgment strictly follows commit, proved structurally rather than by observing a receipt.** A `DEFERRABLE INITIALLY DEFERRED` constraint trigger fails the **COMMIT** with every row already inserted: no receipt reaches the caller, nothing is durable, **and the queued job is gone** — which is the same assertion read as "the job is enqueued inside the save transaction, not after the acknowledgment". A control drives the trigger directly and reads the row back inside its own transaction, ruling out a statement-level failure in disguise. Reverting to acknowledge-then-commit reddens it.
- **The source survives enrichment failure, and both the implementer and the reviewer reproduced the failure rather than asserting it.** The reviewer wrote its own kill — a bare unclassified `RuntimeError` instead of the implementer's classified `JobExecutionError`, its own synthetic note, its own database — ran the real `run_worker` on the real `CAPTURE_JOBS` plane until the bounded retries were spent, and observed the job terminal `failed` with `attempt_count=3`, zero proposals, **and the exact text reading back byte-identical with a matching digest and still findable by search**. The failure is visible in `state` / `last_error_code` / `attempt_count`, not swallowed.
- **Idempotent replay** returns the byte-identical receipt with `created=false`; a changed payload under the same key **conflicts** (`409`) rather than overwriting or silently returning the old receipt. Concurrent racing submissions observe a real `pg_stat_activity` lock wait and the test **fails closed**.
- **Evidence:** head `3ec8d91`. Full suite **4179 passed, 1 failed** against the base's 4171/1; architecture **1831** + that same 1; web **164 passed / 16 files** against 140/13; ruff clean over 552, mypy clean over 166. Single Alembic head `4f1a8b6d92e3`, unchanged. Independently reviewed at the exact head: **clean, zero blockers**, with every figure re-derived by the reviewer on its own PostgreSQL 17.10 container and matching exactly, the D-15 pin reverted and observed red for the intended reason, and the enrichment kill independently reproduced.
- **Invalidation:** capture transaction/contract/pipeline changes invalidate this acceptance.

## NOTEs carried out of WP-07 (§15A — recorded, not blocking)

1. **The D-15 pin is enforced at sign-in, not at session verification, and the repo's own disclosure is thinner than the report.** Not an isolation hole: `resolveSessionPrincipal` requires the `sid` to be live in a process-local in-memory registry, so a `synthetic-b` cookie minted before the pin fails `401` after any restart — verified live by the reviewer. But `web/README.md` and D-15 above describe the closure without saying that verification does not re-check admissibility.
2. **`touchSession` binds a `sid` to liveness but not to its principal.** A `synthetic-b` envelope carrying A's live `sid` reaches A's data — **but only with the HMAC signing secret in hand**, which already permits forging any identity. Outside the threat model and not a defect of this change; recorded as a cheap hardening opportunity (one equality check).
3. **`routes.test.ts` lost its end-to-end "two sessions derive two identifiers" assertion.** Sound justification — the second session *is* the defect — and `gateway.test.ts` asserts `correlationPrincipalId` distinctness at unit level. Consequence: no reachable configuration now admits two identities *and* serves backend data, so the end-to-end property is untestable at this head.
4. **Enrichment-degraded UX does not exist, deliberately.** No capability reports job state, so the capture surface distinguishes durable / acknowledged-not-persisted / refused / unavailable and stops. Inventing a degraded badge would be a claim with nothing behind it. Adding one needs a 16th capability and therefore an `ALTER` migration. The constraint was independently verified real, not an excuse.
5. **Revalidation-on-edit is proved at the persistence/schema level**, against the same `mark_changed_assertions_for_revalidation` that `admit_capture` calls — not end-to-end through `capture.revise`.
6. **Web capture UX is proved by Vitest against the route handlers and the dialog, not by a live browser run.** No manual end-to-end sign-in-and-capture was performed. Browser/E2E validation remains owed under brief §31 before local-candidate completion.
7. **Proposal derivation is text-pattern dependent.** A clean pipeline pass over the reviewer's own note produced 0 proposals where the implementer's note produces ≥1. "Clean run ⇒ proposals" is not unconditional.
8. **The concurrent-admission test drives `admit_capture` on two connections rather than `service.invoke`**, because an `invoke` opens and commits its own transaction. Correct reason, disclosed in its own docstring; the application layer above is covered sequentially.
9. **`test_every_python_root_is_type_checked_or_named` still fails** whenever `web/node_modules` is installed (WP-04 NOTE 6, WP-06 NOTE 9). Unchanged, untouched, not weakened; `git diff` over `tests/architecture/` and `.github/` is empty. It is the single failure in both the base and this head.

### D-17 — OD-COMP-004 resolved, and WP-08 built the offline queue to it

- **Decision:** `OD-COMP-004` is closed as **device/browser-local protected key with explicit stale-session quarantine; document limitations.** Adopted by the Manager under brief §12 as the recommended default stated in completion-plan doc 09 — determinable from canonical sources, therefore not an operator escalation under §13. **The decision is reversible.**
- **Implementation:** AES-GCM 256; key from `crypto.subtle.generateKey(..., extractable: false, ...)`; **one key per `principalId`**, so B's key does not decrypt A's record; stored as a `CryptoKey` in IndexedDB; never exported, serialised, transmitted, or logged; a fresh 96-bit IV per record. **Fails closed with no downgrade** — an environment that cannot hold a non-extractable key refuses the enqueue, and a stored key reading back `extractable === true` is refused rather than used. Proved four ways, including a planted extractable key.
- **The limitations are documented prominently and deliberately under-claimed**, in `web/src/lib/offline/key.ts` and `web/README.md`: not hardware-backed in most browsers (`extractable: false` is the browser's object model, not a TPM); **any same-origin script execution — XSS, a hostile extension, devtools — can obtain the same handle and call `decrypt`, and against that attacker "the encryption buys essentially nothing"**; local profile access is a partial not total defence; no protection against a compromised device; storage is evictable, and an evicted key renders the queued ciphertext permanently undecryptable with no copy anywhere else. Under-claiming was judged the correct direction and there is nothing here to block on.
- **Scope:** `web/**` and documentation only. **No `src/`, no `migrations/`, no new capability, no migration.** Single Alembic head `4f1a8b6d92e3` over 23 revisions, unchanged.
- **The digest concern was raised and closed on evidence, not assumption.** Receipt verification recomputes SHA-256 locally, so a normaliser anywhere between the web tier's `text.trim()` and Python's `digest_of` would have rejected receipts for notes that *were* durably saved — stranding local copies while the server held them. The reviewer traced the whole path in source (`text.trim()` → `buildRequestDocument` → `readCleanBody` → `CreateCapture._text` → `CaptureContent` → `digest_of`) and found no strip, normalise, or truncate; `CaptureContent` uses `.strip()` as a predicate only and refuses over-length rather than truncating. 8/8 vectors matched empirically, including **NFD vs NFC `é`**, the case any normaliser would have collapsed. **No data-integrity finding.**
- **Evidence:** reviewed head `9bce74f`; `95efc6f` is a post-review documentation-only accuracy correction (§15A: docs-only ⇒ no re-review). Full suite **4179 passed / 1 failed**, identical to base — that 1 is the pre-existing `web/node_modules` architecture failure, untouched and unweakened. Architecture **1831** + that same 1. Web **234 passed / 23 files** against a base of 164/16. ruff clean over 552, mypy clean over 166. Independently reviewed at the exact head: **clean, zero BLOCKERs, six NOTEs**, with four controlled reversions observed red by the reviewer's own hand and restored byte-exact.
- **Level of proof, stated exactly:** all seven controls are proved at **unit/integration** level at the queue-and-replay boundary — real queue, real key path, real IndexedDB, real fold and replay, **faked network transport**. **No end-to-end browser run was performed and none is claimed.** Per D-15 no reachable configuration admits two identities *and* serves backend data, so an end-to-end account-switch test is not constructible at this head.
- **Invalidation:** canonical WP-08's own rule — offline key/session/queue schema changes require replay/isolation re-test.

## NOTEs carried out of WP-08 (§15A — recorded, not blocking)

The independent review returned **six NOTEs**. The content of **three** was relayed to this Orchestrator and is recorded verbatim in intent below (items 1–3). The remaining three were relayed to the Manager and **not** to this Orchestrator in detail; they are flagged at the end of this section rather than paraphrased, because inventing their content would be worse than naming the gap. Items 4–6 are **separately-sourced limitations the implementer disclosed and this Orchestrator verified directly** — they are recorded here because they are true and carry forward, not because they are the reviewer's missing three.

### Reviewer NOTEs relayed in detail

1. **The replay Principal check is against a *rendered* identity, not the authenticating session — and the disclosure said otherwise until it was corrected.** `replayQueuedCaptures` compares an entry's stored `principalId` against `currentPrincipalId`, a prop from a server render, while `httpCaptureTransport` posts with `credentials: "same-origin"` and therefore carries whichever cookie the browser holds at that moment. `web/README.md` and the WP-08 commit message both said replay "refuses when the signed-in Principal differs", which overstates it; **corrected in `95efc6f`** in the README and in the `queue.ts`/`replay.ts` docstrings rather than left standing. Not a blocker because the durable cross-principal write is **unreachable at this head** — all three gateway modes were checked; under `local_operator` exactly one Principal is admissible (D-15), unset/`entra` refuse, and the one reachable variant returns `shape: "synthetic"`, which receipt verification rejects so the ciphertext is retained, and which is refused outright when `NODE_ENV=production`. **Discharge condition, verbatim: this becomes release-blocking the moment two identities can hold sessions while the backend serves.** At that point the comparison must be made against the authenticating session.
2. **`needs_reauth` does not count toward `MAX_REPLAY_ATTEMPTS`, and the docstring's claim is untrue of that class.** The attempt bound does not advance for entries parked in `needs_reauth`, so the documented bound does not hold for them. Recorded as written; needs an owning package.
3. **Terminal quarantine needs an owning package, and it needs one *before* a second real Principal can share a browser profile.** Nothing releases a quarantined entry — not even signing back in as the original principal — there is no user-initiated discard, and quarantined entries occupy the queue bound indefinitely. Today that is a disclosed limitation on a path that cannot carry a second real identity. **At the point two identities can share a profile it becomes a genuine data-availability defect with no in-app remedy**, and must be owned before then.
### Implementer-disclosed limitations, verified by this Orchestrator

4. **Browser/PWA validation remains owed under brief §31** (carried forward from WP-07 NOTE 6, and unchanged by this package). No end-to-end browser run has been performed for offline capture, reconnect/replay, account switch, or stale auth. The service worker deliberately does **not** cache HTML documents — a server-rendered page carries `principal.displayName` — so the application does not cold-start offline and offline capture works only in an already-open tab. That refusal traded a feature for an isolation property and is disclosed rather than presented as a capability.
5. **`fake-indexeddb` is not a browser.** It did round-trip a non-extractable `CryptoKey` (`extractable === false` on read-back, decrypting bytes the first handle sealed), so the real IndexedDB code path was exercised — but structured-clone semantics and transaction auto-commit timing are its implementation, not Chrome's, and queue survival across a real browser restart is **not** proved. Added as a devDependency only; no runtime path imports it.
6. **The queue byte bound counts ciphertext, not plaintext** — plaintext plus a 16-byte tag per record. Close enough to be useful, but it is not the note length.

### Three reviewer NOTEs whose text this Orchestrator did not receive

**Recorded as an explicit gap, not silently omitted.** The review returned six NOTEs; three reached this Orchestrator in detail (items 1–3 above) and three did not. They are neither reproduced nor guessed at here. **Consult the WP-08 review report for their exact text before starting WP-09** — none was a BLOCKER, so none gates the merge decision, but they carry forward and the next package should not begin from the assumption that this list is complete.

**Still outstanding after WP-09.** WP-09 did not close this gap: the text of those three NOTEs was not retrieved during WP-09 and is still not recorded anywhere in this brief. Their content remains unknown to this campaign and is deliberately not invented here. The instruction above stands unchanged, now addressed to whoever starts **WP-10**.

**Still outstanding after WP-10, and now three packages old.** WP-10 did not close it either — the text was not retrieved and is still recorded nowhere. Stated plainly rather than allowed to fade: this gap has survived WP-09 and WP-10 unaddressed, and each package that passes it forward makes it less likely anyone still holds the report. The instruction is now addressed to whoever starts **WP-11**, with the added observation that if the WP-08 review report can no longer be located, that fact should be recorded here as a closed-unrecoverable item rather than carried indefinitely as an open one. Note that WP-10's own review, by contrast, relayed **all seven** of its NOTEs in full; the WP-08 relay failure has not recurred.

**CLOSED-UNRECOVERABLE at WP-11, taking the option the paragraph above names.** The WP-08 review report exists nowhere this campaign can reach: the reviewing Worker's context ended with WP-08, no artifact of it was committed to the repository, and nothing in the tree, the brief, or any later review reproduces the three NOTEs. Four packages have now passed the instruction forward without retrieving them, and a fifth attempt would be ceremony rather than recovery. **Their content is unknown and is deliberately not invented here.** What is known and bounds the residual: **none of the three was a BLOCKER** — the WP-08 review returned zero — so no merge decision rested on them, and the three NOTEs that *were* relayed (the replay-Principal comparison, the `needs_reauth` attempt bound, and terminal quarantine) all concern the offline queue and all carry named discharge conditions that remain recorded above. The relay failure has not recurred in WP-09, WP-10 or WP-11, each of which relayed every NOTE in full. **This item is closed. Do not carry it into WP-12.** If the report is ever recovered, reopen it here rather than treating this closure as a finding that the NOTEs did not exist.

### D-18 — WP-09 wired Reveal to real evidence and proved unavailable-vs-empty

- **Decision:** Reveal is **real-backed end to end**. `POST /api/reveal` reaches a new **sixteenth** capability, `knowledge.reveal`, which reads PostgreSQL — no fixture on the default path. Migration **`5e2c7b0a94f6`** revises `4f1a8b6d92e3` and widens `audit_events.capability_is_known` from **26 names to 27** by a forward `ALTER` with the literals frozen in the revision; the downgrade restores exactly 26. **Exactly one Alembic head** over 24 revisions. Reveal reuses the existing `capture_review` purpose — **no purpose was widened** to admit it.
- **Unavailable is a typed outcome, not an empty list, and that was reproduced rather than argued.** The reviewer stood up a disposable database and drove the production writers on a live PostgreSQL server: a *searched* subject that holds nothing returned `state=no_evidence`, `gap=None`, row counts `(0, 0, 0)`, 1 version, 1 with completed derivation; an *unsearched* subject returned `state=unavailable`, `gap=derivation_has_not_completed_for_every_version`, row counts `(0, 0, 0)`, 1 version, 0 with completed derivation. **Identical row payloads, distinct states** — which is the whole claim. The web tier renders `reveal-unavailable` versus `reveal-no-evidence` over byte-identical empty arrays, branching on `state` and never on a length.
- **Cross-Principal isolation is proved at the repository/query boundary, and explicitly NOT end to end.** D-15 pins the web tier to exactly one Principal under `local_operator`, so a two-identity end-to-end claim is not constructible at this head. What was proved: the reviewer stood up two Principals holding real evidence and attempted the breach directly — B revealing A's capture returned `None`, B revealing A's assertion returned `None`, so **foreign is answered identically to absent for both subject kinds** and the call is not an existence oracle, with a non-zero control in the same run proving the query returns rows when it should. A static architecture guard holds, per statement, that every statement in `persistence/reveal.py` reaches the partition. **This is the exact level of proof; it is not to be upgraded in any later restatement.**
- **Six acceptance controls: 1, 2, 3, 4 and 6 proven; control 5 is partial.** Reveal publishes coverage and freshness and Library forwards them, but the Library *page* is still a `NotConnected` placeholder, so nothing renders them to a human yet. Stated as achieved, never implied.
- **Evidence:** head `3f63be5`. Full suite **4246 passed / 1 failed** against base `d989809`'s **4179 / 1**; architecture **1860 passed** plus that same 1; web **243 passed / 24 files** against a base of 234 / 23; ruff clean over **559** files, mypy clean over **170**. That single failure is the pre-existing `test_every_python_root_is_type_checked_or_named` (`web/node_modules`, WP-04 NOTE 6) — untouched, unweakened, and **not in the diff**. The reviewer re-derived the base figures independently in a throwaway worktree and matched exactly, and planted **five** controlled reversions by its own hand — including dropping `principal_scoped` from the ownership lookup and collapsing the derivation check — observed each red for the intended reason, and restored every file SHA-256-verified.
- **Invalidation:** canonical WP-09's own rule — any retrieval or disclosure contract change invalidates the evidence UX tests.

## NOTEs carried out of WP-09 (§15A — recorded, not blocking)

1. **The "unconstructible empty-success" claim was false and is corrected in this package.** `Reveal.__post_init__` enforces no such rule — a `no_evidence` reveal holding a version whose derivation has not completed constructs successfully — and `RevealView` omits it too. The guarantee is at the **repository assembly path**, `_state_and_gap` in `infrastructure/persistence/reveal.py`, which orders the derivation test ahead of `NO_EVIDENCE`; runtime behaviour is correct and was reproduced against real PostgreSQL, and the acceptance criterion is met on every path this build exercises. What does not exist is the claimed defence-in-depth, and **it still needs an owner**: there is no constructor rule and no test (`test_a_no_evidence_reveal_cannot_hold_an_incomplete_version` does not exist), so a future assembly path bypassing `_state_and_gap` could produce the shape the docstrings called impossible. Corrected in wording, not in code — deliberately, under the lean cadence.
2. **`unavailable_evidence` closure is by convention at one call site, not by type.** `application/disclosure.py:273` types it `tuple[str, ...]` while its sibling field is `tuple[Limitation, ...]`; the closed vocabulary holds only because `service.py:1293` passes `(reveal.gap.value,)`. **Not a v1 contract break** — the field is pre-existing and defaults to `()`.
3. **No Trace surface exists.** There is no `/api/trace` and no `web/src/**/trace*`; the derivation trace is delivered inside Reveal instead. The Library page remains `NotConnected`. Honestly disclosed, and outside the verbatim acceptance criteria, which name no Trace surface.
4. **The Review surface was not revisited, but control 2 is not unaddressed there.** The pre-existing WP-08 workbench already renders proposed versus accepted distinctly — a gold "Proposed" badge, "Proposal — not asserted", and "Recorded: <disposition>" only after a decision. `review/page.tsx` still renders `syntheticReviewCases`, which fails closed in a default build.
5. **Latent duplicate-version risk in `_versions`.** `capture_stage_results` is unique only on `idempotency_key`, so a stage-config change would create a second `(version_id, PERSIST_PROPOSALS)` row and the outer join would emit duplicate `RevealedVersion` entries. Unreachable at this head and it **fails safe**: a duplicate forces `UNAVAILABLE`, never a false `NO_EVIDENCE`.
6. **The Reveal payload is unbounded by subject.** It is bounded per version by `MAX_CAPTURE_CHARACTERS = 100_000` but unbounded in version count. Low significance under D-15, where the only party who can inflate it is the party who receives it.
7. **A successful reveal's envelope coverage is about enrollment, not evidence.** `_knowledge_reveal` uses `unenrolled_disclosure`, so `coverage.state` is `not_enrolled` and reports `"complete"`; the evidence coverage lives in the payload as `versions_with_completed_derivation`. Consistent with every other capture capability, and the `unavailable` branch does set `coverage.state` correctly. The dialog renders `disclosure.coverage` and `limitations` but **not** `disclosure.authority`.
8. **Subject-kind coverage is honest.** Only `cap_` and `asrt_` are traversable; every other **known** `IdKind` prefix returns a typed `unavailable` with `SUBJECT_KIND_NOT_COVERED`, answered identically for both Principals so it discloses nothing. An **unknown** prefix is a `400` validation error — a deliberate shape-versus-coverage split, not a silent empty.

### D-19 — WP-10 built the remote ingress as a second credential plane, and deliberately did not create WP-08 NOTE 1's condition

- **Decision:** an authenticated remote capture ingress at `POST /remote/v1/capture.create`, reached with `Authorization: ClientCredential <client_id>:<secret>`, **off by default** behind `MY_PA_REMOTE_INGRESS_ENABLED`. It reaches `capture.create` through `ApplicationService.invoke` — the same authorization, the same `admit_capture`, the same four inserts in one transaction, the same `UNIQUE (principal_id, idempotency_key)`, the same receipt. **No new capability**: the seat stays at 27 names and `audit_events.capability_is_known` is untouched. Migration **`7a1e5f3c9d24`** revises `5e2c7b0a94f6`, creates `knowledge.capture_clients` (principal-partitioned) and widens `capture_transport_is_known` and `capture_trust_state_is_known` by forward `ALTER` with frozen literals; the downgrade restores both and drops the table. **Exactly one Alembic head** over 25 revisions.
- **The transport is provenance, not a second model, and that is measured rather than argued.** A remote submission and a local one carrying byte-identical text were both admitted on live PostgreSQL and every row both wrote was compared column by column with per-request identifiers, digests and clock readings removed: `captures`, `capture_versions`, `capture_receipts` and `capture_jobs` are **equal**, and `capture_submissions` differs in exactly `transport` and `trust_state` and in nothing else. The transport reaches the row through `ApplicationService.invoke`'s own parameter — the same trust channel the acting `Principal` arrives on — and no command, payload or envelope field can carry it. It is deliberately **outside** `payload_digest`, so a device that retries over the other transport replays rather than conflicting; that crossing is a test.
- **Credential minting is an operator command, not a capability** (`AGENTS.md` §8.2, `D-42`). `apps/cli/clients.py` mints, lists, revokes and reports; it names no `Capability`, builds no `ApplicationService`, and its permitted persistence surface is registered in `tests/architecture/test_operator_commands_are_not_capabilities.py`. The secret is 32 bytes of CSPRNG output, printed **once**, stored only as SHA-256 (justified on the input's entropy rather than assumed), compared with `hmac.compare_digest`, and absent from every type a listing can return. There is **no `--principal-id` flag** and there will not be one.
- **WP-08 NOTE 1's condition was not created, and the avoidance is executable.** Under `local_operator` a client binds to the one process Principal and a row naming another is refused — at minting *and* at every authentication, so a row written by hand or before a mode change is refused rather than served. The ingress mints **no browser session**: the Python gateway reads no cookie, issues none, and holds no session secret, and an architecture guard asserts that over the composition root, the transport and the client plane. No `web/src` module can name the ingress path, the scheme, the table or the `cclt_` prefix, so a browser cannot obtain a device credential and a session cookie cannot become one. `web/` is **byte-unchanged** by this package, and the four files carrying the `D-15` pin and WP-08's queue semantics are SHA-256-pinned in that same guard so a quiet edit fails the build.
- **Level of proof, stated exactly, and corrected by the independent reviewer where the implementer over-stated it.** Controls 1, 3, 4 and 6 are proved at **live-PostgreSQL integration** through the real composition and over a real loopback socket. **Control 5's committed proof is weaker than this entry first claimed:** the disabled-handler test is at **contract** level (a real socket, but a stub authenticator over a fake unit of work, `tests/contract/test_remote_capture_ingress.py`) and the composition half is a **unit** test; the join — a real `build_gateway_runtime` with the switch unset, refusing `501` over a socket against live PostgreSQL — is **not in the tree**, and was performed by the reviewer rather than committed. **Control 1 carries one further precision:** the column-by-column comparison drives the *local* half through `ApplicationService.invoke` directly rather than over a socket (disclosed at `tests/capture/test_remote_capture_is_one_durable_transaction.py:184-199`); only the remote half crosses a socket, so "both over a real loopback socket" would be an over-statement. Control 2 is proved at the **composition-root, persistence and transport level** — the reviewer raised it from the implementer's stated level by demonstrating all six breach attempts through a real socket — but it is **not** an end-to-end two-identity claim, because no reachable configuration admits two identities holding sessions while the backend serves, and it **is not to be upgraded in any later restatement**. No browser run was performed; brief §31 remains owed.
- **Evidence:** head `4799695`. Full suite **4376 passed / 1 failed** against the base's **4246 / 1** — that 1 is the pre-existing `test_every_python_root_is_type_checked_or_named` (`web/node_modules`, WP-04 NOTE 6), untouched and not in the diff. Architecture **1900** plus that same 1, against 1860. Web **243 passed / 24 files**, identical to base and untouched. ruff clean over 572 files, mypy clean over 174. Ten controlled reversions were planted by the implementer. **Independently reviewed at the exact head: clean, zero BLOCKERs, seven NOTEs.** The reviewer re-derived every figure above and matched each one — including reproducing the base's single failure after discovering its own symlinked `web/node_modules` had masked it, which it recorded rather than let stand as a discrepancy — and planted **seven** controlled reversions by its own hand, including the D-13/D-14 `metadata.principal_id` plant (6 red: 2 architecture guards + 4 behavioural) and a reversion that made the ingress log the request document and the presented credential (red for both the note text **and** the client secret on the failed-authentication path, which is what establishes the redaction plant as non-vacuous). All restorations SHA-256-verified; no disabled guard remains anywhere in the tree. The reviewer notes it could not verify the implementer's ten reversions, only that none was left behind.
- **Invalidation:** any change to the ingress route, the credential plane, the client lifecycle, or the `D-15` pin invalidates this acceptance.

## NOTEs carried out of WP-10 (§15A — recorded, not blocking)

The independent review returned **zero BLOCKERs and seven NOTEs**. Unlike WP-08, **every reviewer NOTE reached this Orchestrator in full and is recorded verbatim in intent below**, in its own subsection. The ten items after them are **implementer-disclosed limitations**, verified by this Orchestrator; they are a separate list and are not the reviewer's findings.

### Reviewer NOTEs (independent review at head `4799695`)

**R1. After a switch to `entra`, a client minted under `local_operator` keeps working and there is no operator command that can revoke, list, or even count it.** Reproduced live on the reviewer's own PostgreSQL, twice. `Settings(auth_mode=ENTRA).admissible_client_principal_id()` returns `None`; `admit_client_binding` reads `None` as "any authenticated Principal"; so `remote_client_authenticator` resolves the stale credential and returns `Principal(principal_id=<the local operator>, kind=OPERATOR, authenticated=True)`. Meanwhile `apps/cli/clients.py revoke` and `list` both **refuse with exit 1** — `_bound_principal` raises in that mode — and `status` prints `clients not counted; this process binds no Principal a shell can name`. The operator's only remaining levers are `MY_PA_REMOTE_INGRESS_ENABLED=false`, which disables the whole ingress, or hand-editing the row. This also sits against two invariants the codebase states about `entra` in its own words: `bootstrap/gateway.py` says that mode "issues **no** process principal" and that `None` is used "so a transport composed with the wrong field in the authenticated mode fails at composition instead of serving every caller as the operator", and `Settings` validation exists to prevent a mode that "would serve every request as the local operator" — yet the ingress can serve *as* the local operator in exactly that mode, via a stored row. **Not a BLOCKER:** it requires an operator-performed mode change with clients already minted and the ingress deliberately enabled; the credential still acts only as precisely the Principal it was bound to, so there is no cross-Principal read, no privilege change, and no isolation breach; and the kill switch remains. **Implementer NOTE 2 below discloses only that *registration* has no reachable path in `entra`** — it does not disclose that revocation and listing are equally unreachable, nor that already-minted credentials go on being served. **Needs an owning package before `entra` is ever activated alongside an ingress.**

**R2. Two operator-facing documents still tell a reader the gateway requires no credential.** `docs/operations/mcv-limitations.md:54-55` ("No credential is issued, read, or required; no TLS is configured; no ingress is activated") and `ops/runbooks/gateway-operations.md:53-55` ("the gateway issues, reads, and requires no credential, and configures no TLS"). Both were already stale after WP-05's bearer plane; WP-10 adds a *second* credential plane to the same process and corrects neither. The omission is inconsistent rather than deliberate: the identical sentence in `README.md` **was** corrected in this package, and `apps/gateway.py`'s own docstring corrects itself in place. The runbook is the file `README.md` names as "covers running it". Implementer NOTE 3 says no runbook was *added*; it does not say the existing one now carries a false statement about the process this package changed.

**R3. `capture_clients` is not asserted empty after a capture save.** `tests/pipeline/test_save_does_not_wait.py` puts it in a new third category, `OPERATOR_OWNED_CONTROL_PLANE`, used only by the coverage-completeness test at line 379; the `_rows(connection, table) == 0` assertions at lines 156 and 233 iterate `DOWNSTREAM_OUTPUTS` only. The stated reason — the emptiness means something different ("nothing in a request can write here at all" versus "the pipeline has not run yet") — is sound and worth preserving, but the consequence is that "a capture save does not mint a credential" is asserted nowhere in that module. It is covered indirectly by the redaction row sweep and by the operator-command architecture guard. One extra loop would close it.

**R4. `infrastructure/persistence/capture_clients.py` joins the partition guard as `PER_MODULE_ONLY` rather than `STATEMENT_LEVEL`, although three of its four statements do reach the partition.** The registered reason — `authenticate_client` *derives* the Principal, so a partition predicate there would need the answer in order to ask the question — is genuine and matches the `jobs.job_principal` precedent exactly. But the classification is coarser than the module warrants: a future fifth statement added without a predicate would be absorbed by the module-level exemption rather than caught, which is the precise failure mode that guard's docstring says `jobs.py` already exhibited once. A statement-level entry with one named exception would be strictly tighter.

**R5. `_without_caller_identity` checks only the top level of `payload`.** `src/my_pa/adapters/http/app.py:454-458` tests `_IDENTITY_FIELD in payload` for the payload mapping itself; it does not walk nested mappings, whereas its sibling on the other plane (`domain/identity/user_account.py:194`) explicitly "checks the payload and any nested mappings". Unreachable today for a sound reason: `_command` builds `CreateCapture(**payload)` and an unknown key raises `TypeError` → 400, and `CreateCapture` declares no mapping-typed field for a nested object to live in. A future capture command accepting a structured field would reopen the asymmetry, and the shallow check would not notice.

**R6. `test_the_created_table_matches_the_live_declaration` compares constraints by subset, not equality.** `tests/schema/test_capture_client_migration.py:286` asserts `named <= live`. Columns are compared by full equality; constraints are not, so a constraint present on the server but absent from `tables.py` would pass. The docstring's wording ("every named constraint on the declaration has to exist on the server") is literally accurate, but a reader scanning for drift protection may take it for the two-directional claim the column check actually makes.

**R7. The ingress adds no rate limiting, and neither has anything else.** Not a regression and not in the acceptance criteria; recorded only because the ingress is the first address in this build intended to be reached by a device the operator is not sitting at, and implementer NOTE 4 correctly says publishing it is an operator act. Whoever writes the TLS-terminating proxy owns this.

**Two questions the reviewer was asked to judge and closed as no-finding, on evidence.** *SHA-256 rather than a slow KDF is correct here, not a shortcut* — the secret is `secrets.token_urlsafe(32)`, 256 bits of OS CSPRNG; a KDF exists to make a low-entropy human-chosen secret expensive to iterate, and against a uniformly random 256-bit preimage no iteration count buys anything measurable while costing every ingress request a deliberate delay. Comparison is `hmac.compare_digest`. *The timing residue in `secret_matches` (implementer NOTE 6) is real and negligible* — `client_id` is `secrets.token_hex(16)`, 128 bits, so there is nothing to enumerate, and `authenticate_client` runs the state test after the secret comparison with both always evaluated, so a revoked holder is not answered faster than a wrong-secret holder.

### Implementer-disclosed limitations, verified by this Orchestrator

1. **`capture_submissions` records *that* a registered client submitted, not *which*.** `registered_client_id` is still absent (`D-74`'s column). The binding a reader needs — client to Principal — is on the client row, and the audit event records the Principal, so nothing is unattributable; what is missing is per-client forensics after a revocation. Adding the column means a forward `ADD COLUMN` on a merged table and a freeze-out in `1a4c9e77b2d5`, which was judged out of proportion to the acceptance criteria. Needs an owning package if per-client attribution is ever wanted.
2. **`entra`-mode client registration has no reachable operator path.** The binding rule is implemented for both modes and unit-proved, but `apps/cli/clients.py` **refuses** in `entra` mode because a shell carries no bearer token — the same refusal `apps/gateway.py mcp` makes. So in that mode the ingress can serve no client, because none can be minted. Closing it needs an authenticated registration surface, which is a credential-minting HTTP path and therefore an operator decision.
3. **No runbook was added.** `ops/runbooks/README.md` binds a procedure to having been executed, and no operator has run the mint-and-submit sequence against a published address. The command's own docstring and `README.md` carry the procedure instead; a runbook should follow the first real operator run.
4. **The ingress is loopback-only and no live activation was performed.** `apps/gateway.py` still binds `127.0.0.1` as a constant with no flag. Turning the switch on exposes nothing; reaching the ingress from a device needs a TLS-terminating proxy or tunnel the operator runs, which this package deliberately does not build or document as a recipe.
5. **iOS Shortcut compatibility is a contract-shape claim, not a tested one.** The ingress takes the canonical envelope plus a payload, refuses a query string, and needs one header — a shape a Shortcut can build. **No Shortcut was written and none was run.** The claim is that the contract suits one, and it is not stronger than that.
6. **A wrong `client_id` and a wrong secret are equalised in the comparison, not in the query.** `secret_matches` compares against a fixed digest when the row is absent, so the comparison takes one path; the database lookup that precedes it does not. A client identifier is CSPRNG output, so there is nothing to enumerate, but the residue is real and is stated rather than papered over.
7. **The remote plane refuses a caller-stated `principal_id` and the other transports do not.** That asymmetry is deliberate and narrower on the plane that matters, but it means the ingress document is not byte-identical to a `/v1/capture.create` document: a caller must omit the field the contract otherwise requires. Disclosed in the transport's docstring and in the tests.
8. **`test_every_python_root_is_type_checked_or_named` still fails** whenever `web/node_modules` is installed (WP-04 NOTE 6, WP-06 NOTE 9, WP-07 NOTE 9). Unchanged, untouched, not weakened, and not in the diff. It is the single failure in both the base and this head.
9. **Six further `render_as_string(hide_password=False)` sites were added**, two in each of the three new database-tier modules, in the disposable-database fixture shape every other suite already uses. Same family, same deferred owner as backlog item 6 and WP-05 NOTE 6. Nothing logs any of them, and the value carries no password in this environment.
10. **The full suite gained roughly 130 tests and about seven seconds of database-tier time.** Measured at 517s against the base's 510s. Well inside the FULL target; recorded so a later package re-measures rather than assumes.

### D-20 — WP-11 completed the continuity objects and made Pulse a derivation rather than a list

- **Decision:** Commitments, Decisions and Tasks — which existed at **no layer** at the base head, not as tables and not as domain types — are implemented as principal-partitioned objects alongside an append-only `continuity_lifecycle_events` table that carries lifecycle/closure evidence *and* project/context associations in one place. A **Commitment is modelled as a social obligation** carrying a counterparty reference, per completion-plan doc 10's rule that a commitment "is a social obligation, not only a generic task", rather than as a task with a different name.
- **Three new capabilities, one migration.** `continuity.pulse`, `continuity.situations` and `continuity.projects` take `Capability` from **16 to 19** and `audit_events.capability_is_known` from **27 names to 30**. This is the seam WP-06 measured and named: the Pulse, Situation and Project read models were already real and principal-scoped, and the *only* thing missing was a capability seat, which needs a forward `ALTER` and therefore a migration. WP-11 wrote it. Migration **`8f2b6c4d1a37`** revises `7a1e5f3c9d24` with the literals frozen in the revision and no live enum import; the downgrade restores exactly 27 and reverses every other change. **Exactly one head** over **26** revisions.
- **Pulse was a list and is now a derivation, and this is the package's sharpest product claim.** At the base head `generate_pulse` was a plain `SELECT ... ORDER BY priority DESC, generated_at DESC` over a hand-populated table — nothing derived an item from anything. It now derives from the Principal's **accepted** continuity state: a commitment approaching or past its due moment, a decision blocked on a named authority point, a situation with an unmet obligation. Each item carries a closed `reason_code`, the `basis_refs` that justify it, a consequence and a next step. **The decisive control is structural rather than behavioural:** `pulse_items` gained CHECKs requiring a known `reason_code` and a **non-empty** `basis_refs`, so a Pulse item with no evidentiary basis is *unstorable*. An activity feed cannot be persisted here, not merely discouraged by convention.
- **The independent reviewer tested that claim rather than accepting it, and reached the same verdict.** Three accepted objects created seconds before the read — none carrying a due moment, authority point or obligation — produced **no item at all**, where a feed would have put all three at the top; the reviewer calls this the decisive result and this Orchestrator agrees. An accepted commitment nine days past due ranked **first** while being the *oldest-created* object, the inverse of reverse-chronological. `generated_at` was identical across all items, so recency cannot order them even accidentally. `PulseReasonCode` has six members and **none is expressible as "recent"** — the server refuses `reason_code='recently_updated'`. **§22 holds:** no relationship score, sentiment, personality scoring, people ranking or cross-Principal aggregation anywhere in the diff; `priority` is a bounded 1..10 rank on an *item*, and a counterparty appears only as an opaque reference that is never an input to the rank.
- **No automatic consequential promotion, proved three ways and stronger than the implementer claimed.** The derivation module imports no SQLAlchemy and builds no write; exactly one method assigns `ACCEPTED`, and only after reading the review plane; six `derive_pulse` calls across two Principals changed **zero rows** in all eight continuity tables. The reviewer raised the stated level: **nothing in `src/` or `apps/` calls `accept` or `propose_*` at all, and `ContinuityRepository` is exposed on no `UnitOfWork` property**, so the promotion path is unreachable from any capability rather than merely guarded.
- **Accepted-only is a schema guarantee, not a convention.** `accept` requires a `capture_review_decisions` row **in the caller's partition** whose disposition accepted; a rejection, another Principal's decision and an absent identifier are all refused **identically**. Raw SQL setting `evidence_state='accepted'` with a NULL review decision raises `IntegrityError` on the live server. With three proposals present, `derive_pulse` returned **0** items; after proper acceptance, 3.
- **`migrations/env.py` moved to `transaction_per_migration=True`.** Whole-run atomicity is given up and **per-revision atomicity is retained** — the reviewer proved it by driving the `pulse_items` refusal and watching three already-created tables roll back cleanly, leaving the database byte-identical at the prior head. No test depended on whole-run atomicity. **The change is sound; its stated justification is not reproducible** — see NOTE 1 below, which is the one place this package's own documentation overstates its evidence.
- **Deliberately not done, and these are decisions rather than omissions:** **no `/api/trace` surface** — WP-09 delivers the derivation trace inside Reveal and that is the product's trace answer, so inventing a second one would answer a different question; the **Library page remains `NotConnected`** (WP-09 NOTE 3, unchanged); and **Commitments, Decisions and Tasks have no read capability of their own**, reaching a human through Pulse instead. Their *write* path is reachable only by direct construction and is wired to no capability.
- **Evidence:** head `21b049b89aa4c0b6c1d69e7bfa681a052c491e1a`, PR **#64**, stacked on #63. Full suite **4520 passed / 1 failed** against the base's **4376 / 1**; architecture **1928** plus that same 1, against 1900; `tests/schema tests/database` **280**; web **246 / 24** against 243 / 24; ruff clean over 348 linted / 580 formatted; mypy clean over **177**. That single failure is the pre-existing `test_every_python_root_is_type_checked_or_named` (`web/node_modules`, WP-04 NOTE 6) — untouched, unweakened, not in the diff. **Independently reviewed at the exact head: clean, zero BLOCKERs, three NOTEs.** The reviewer re-derived every figure on its own PostgreSQL 17.10 and matched each, **attempted 28 breaches across the six controls and had all 28 refused**, and planted **six** controlled reversions by its own hand, observing each red for the intended reason and restoring every file SHA-256-verified. No disabled or planted guard remains; 0 tests removed, 53 added, all count assertions still exact `==`.
- **Level of proof, stated exactly and not to be upgraded in any later restatement:** cross-Principal isolation is proved at the **live-PostgreSQL repository/query** boundary, two Principals, foreign answered **identically to absent** across ten operations with a non-zero control in the same run. It is **not** an end-to-end two-identity proof and none is constructible at this head, because D-15 pins the web tier to exactly one Principal under `local_operator`. The four SHA-256-pinned D-15 files are byte-unchanged and the pin guard is untouched.
- **Invalidation:** canonical WP-11's own rule — continuity schema or acceptance-rule changes require review.

## NOTEs carried out of WP-11 (§15A — recorded, not blocking)

The independent review returned **zero BLOCKERs and three NOTEs**, and **all three reached this Orchestrator in full** and are recorded below.

1. **`migrations/env.py`'s stated measurement does not reproduce.** Its docstring asserts the single-transaction run failed with `out of shared memory` once four tables were added, and that the chain was "already within a dozen relations of the ceiling". On a default PostgreSQL 17.10 (`max_locks_per_transaction=64`, `max_connections=100` → 6400 slots) the reviewer measured single-transaction `upgrade head` at **exit 0, peak 4712 locks** and `downgrade base` at **exit 0, peak 3359** — real headroom of roughly 1,700–3,000 slots, not a dozen relations. The likely true cause is cluster-wide lock pressure from a **concurrent** database run, since the lock table is shared and 2 × 4712 exceeds 6400 — which is precisely the hazard this campaign's own operational lessons warn about. **The change itself is correct** — holding 74% of the cluster lock table in one transaction is genuinely fragile — and per-revision atomicity was proved. Only the causal sentence is wrong, so this is a documentation-accuracy defect, not a migration-coherence one. Whoever next touches that file should correct the docstring rather than inherit the claim.
2. **`accept()` binds to *an* accepting review decision, not to *this object's*.** It requires only a `capture_review_decisions` row in the caller's partition with an accepting disposition; it does not require that decision to concern the object being promoted. The reviewer observed **one review decision promote six unrelated continuity objects**. So `accepted_by_review_decision_id` records *that* a review happened, not that *this object* was reviewed. Not a blocker: acceptance is never automatic, the partition check holds, and **no production code path reaches `accept` at all**. **This must be closed before the continuity write plane is wired to any capability** — at that point it becomes a real accepted-only defect.
3. **The campaign's stated test baselines are environment-sensitive and not reproducible from a clean checkout.** The "1 failed" in every figure from WP-04 onward depends on `web/node_modules/flatted/python/flatted.py` — one vendored Python file inside an npm package — making `web` look like an unchecked Python root. A fresh clone or a git worktree has no `node_modules`, so the suite reports **4377 / 0** at base and the test *passes*. The reviewer proved the cause both ways: the test passes in a bare base worktree and fails there the moment that one file is restored, and the guard file is byte-identical at base and head. Totals reconcile exactly (base 4377, head 4521, **+144, zero regressions**). Anyone re-deriving these figures needs `npm install` to have run first, and this brief has never said so.

### D-21 — WP-12 found the Relationship Intelligence machinery already correct, and completed it by proving it and closing the one real hole

- **Decision: WP-12 changed no production code, deliberately.** The diff is **test-only plus a one-line doc count** — `+1327/-1` across five files, **zero** `src/`, `apps/` or `migrations/` changes. This is a defensible outcome for a *completion* package, not an evasion, and the independent reviewer tested that proposition adversarially before agreeing. WP-04 and WP-11 had already built the plane: 19 `relationship_*` tables, a 1228-line `SqlRelationshipRepository`, and a domain `IdentityResolution` that already mandates `review_case_id` + `decision_id` and retains **both** retained and prior person IDs precisely so a split can restore the prior identity without reconstructing it from source content. The remaining work was proof, and one structural guard that genuinely did not exist.
- **The anti-scoring guard is the package's substantive deliverable, and the brief's own premise about it was half wrong.** The Orchestrator's assignment said no §22 scoring guard existed. In fact `tests/relationship/test_relationship_domain.py` already froze every relationship column and dataclass field as a **closed allow-list** — which makes a scoring field *visible* but not impossible, because the allow-list and the schema are both source: one commit adding `relationship_score` to `relationship_people` **and** to `EXPECTED_TABLE_COLUMNS` leaves that guard green. What was missing was the **semantic deny rule**. `tests/architecture/test_relationship_scoring_surface_is_denied.py` (449 lines) adds it over live SQLAlchemy columns (18 tables), relationship dataclass fields (17), relationship `StrEnum` member names **and values**, and — decisively — **the allow-list constants themselves, read via `ast`**, so the two guards cannot both be satisfied by a scoring field. 36 deny patterns across composite judgement and protected traits, matched on snake_case and camelCase **tokens rather than substrings**, so `trace_id`, `identifier`, `message` and `language` survive.
- **Non-vacuity was proved twice over, by two different hands.** The implementer planted a `relationship_score` column and observed the guard red. The **reviewer independently planted four more** — a live column, a camelCase dataclass field (`relationshipWarmthRating`), an enum member whose **name and value were both caught** (`RAPPORT_SIGNAL = "sentiment_positive"`), and the laundering path: with the column planted it *also* widened `EXPECTED_TABLE_COLUMNS`, watched the closed-vocabulary guard go green (`17 passed`), and watched the deny rule catch it anyway. Non-vacuity is now **permanent rather than one-off**: 48 prohibited names must each be detected on every build.
- **Two documented narrowings, verified honest rather than convenient.** `disposition` is deliberately **not** denied — it is the live `accept`/`reject`/`defer` review outcome on `relationship_identity_review_decisions`, and denying the stem would redden the very governance this package exists to require. `priority` **is** denied on the relationship surface while `pulse_items.priority` is untouched, because the scan is prefix-scoped and that column is bounded item ordering, not a person attribute.
- **The WP-11 NOTE 2 defect pattern does not exist on this plane, and this was proved by reversion rather than by reading.** `apply_resolution` enforces a **seven-way binding** strictly stronger than the continuity gate's: decision joined to *its own* case, latest sequence, accepting disposition, matching action, matching retained/prior people, the exact reviewed candidate people, and the exact reviewed observation set. The reviewer replaced the decision-to-case join with the partition-only predicate the continuity plane uses and the test reddened — arriving as `psycopg.errors.RestrictViolation` because `migrations/versions/20260804_7f2a9d6c4e18_…py:255-284` enforces the same sentence in a trigger. **Defence in depth at both layers.** A decision from an unrelated review case cannot authorize a merge.
- **Merge/split reversibility is proved at the read model, not merely at the link table.** Merge two people under an accepted review, then split: the prior person is readable again and compares equal **as a whole `PersonProfile` value** to the pre-merge read — observations, coverage, evidence, timeline and aliases, with only the read's wall clock normalised away — while both corrections remain on the record (`link, link, merge_person, split_person`). The reviewer made it fail deliberately by deleting the split call, confirming it measures the split. Merge without governance is refused three ways (unrelated case's decision, superseded accept, never-decided review), identically.
- **§22 holds absolutely.** No scoring, sentiment, ranking or trait-inference field exists anywhere in the diff — which adds **zero** `Column(...)`, zero dataclass fields and zero enum members. Every §22 token in the diff is confined to the guard module's deny patterns and its 48 planted-violation parameters asserting those names are *rejected*.
- **Level of proof, stated exactly and not to be upgraded in any later restatement:** cross-Principal identity isolation is proved at the **repository/query boundary**, two Principals, foreign identities *and foreign authority* both answered identically to absent, with a non-zero control in the same run. It is **not** end-to-end, and none is constructible at this head for two independent reasons: **no capability seat reaches the relationship plane at all** (19 seats, none relational), and D-15 pins the web tier to one Principal. The reviewer found a second independent barrier the implementer had not claimed — removing the decision partition predicate still left the cross-Principal test passing, because the decision→case join blocks it alone.
- **Evidence:** head `5ca88cd645803938790a45685f3aafd3c1c148c8`, PR **#65**, stacked on #64. Full suite **4609 passed / 2 failed / 5 errors**; architecture **2017 / 1**; `tests/schema tests/database` **279 / 1 / 5 errors**; web **246 / 24**; ruff clean (582 formatted); mypy clean over **177**; single Alembic head **`8f2b6c4d1a37`**, no migration added, capability seats still **19 with none relational**. **Independently reviewed at the exact head: clean, zero BLOCKERs, nine NOTEs.** The reviewer re-derived every baseline and the arithmetic closes with **zero unexplained movement**: architecture 2017 − 89 new = 1928; schema+database 285 − 5 new = 280; full 4520 + 95 new − 5 errored − 1 newly-failed = 4609. **Zero removed lines across the whole test diff** — `git diff -- tests/ | grep "^-"` returns exactly four `--- a/…` file headers and nothing else. Seven controlled reversions were run by the two agents combined and **every one was reverted**; tree verified clean.
- **The second suite failure is a pre-existing time bomb, not a regression, and it is now permanent.** `tests/database/test_reveal_isolation.py` hardcodes `WHEN: Final = datetime(2026, 8, 10, 12, 0, tzinfo=UTC)` at line 104 and inserts `completed_at=WHEN` against `started_at=now()`, violating the `a_stage_completes_after_it_starts` CHECK once real time passed that instant — which it did **mid-package, on 2026-08-10 at 12:00 UTC**. Decisive evidence: the file's blob SHA is **identical at base and head** (`97eff2039953ee831c78a0ea03cb3a0881cd0f6c` twice), and the diff touches no `src/`, `apps/` or `migrations/` path that could reach it. See NOTE 9 — it needs an owning package, because from here it reddens every run.
- **Invalidation:** canonical WP-12's own rule — relationship identity/schema/policy changes invalidate RI review.

## NOTEs carried out of WP-12 (§15A — recorded, not blocking)

The independent review returned **zero BLOCKERs and nine NOTEs**, all recorded below.

**⚠️ NOTEs 5, 6 and 7 are one coupled obligation, not three separate items.** Each is latent *only* because no capability seat reaches the relationship plane. Whoever wires the **first** relationship capability inherits all three simultaneously and must discharge them together, before wiring — this is the relationship plane's exact analogue of WP-11 NOTE 2's condition on the continuity write plane.

1. **The deny rule has reachable bypasses.** Probing `denials()` directly, these pass: `signal`, `index`, `fit`, `vibe`, `stature`, `centrality`, `pagerank`, `proximity`, `familiarity`, `synergy`, `alignment`, `network_value`, `trait`, `traits`, `characteristic`, `attributes`. Sharpest: **`weightings` passes while `weighting` is denied** — the pattern omits the `-ings` plural. The literal word `trait` is not denied despite protected-trait inference being the prohibition. NOTE rather than BLOCKER because the *stated* control holds (`score`, `rating`, `rank`, `tier`, `grade`, `trust`, `loyalty`, `reputation`, `compatibility`, `affinity`, `warmth`, `propensity` are all caught) and any bypass name must still widen the closed allow-list, which the deny rule also scans and a human reviewer reads.
2. **Guard scope is prefix- and package-bound.** `relationship_table_columns()` scans only `METADATA` tables prefixed `relationship_`; `_relationship_modules()` only `src/my_pa/domain/relationship/*.py`. A people-scoring column on a differently-named table, or a relationship dataclass declared elsewhere, is unscanned. Partly mitigated by hard counts (18 tables / 17 models) that redden on any addition inside scope. `src/my_pa/contracts/v1/` is not scanned at all — vacuous today, but the completion plan's WP-9 criterion says "schema **or contracts**".
3. **Free-text carriers are unreachable by structural guards.** `relationship_events.context` and `relationship_aliases.value` can carry a score as *content*. No JSONB bypass exists today: **no `relationship_*` table has a JSON/JSONB column** (verified across all of `tables.py`).
4. **A docstring overstates its own assertion.** `tests/architecture/test_relationship_identity_does_not_reach_the_continuity_accept_gate.py:151-152` claims "nothing in `src/` or `apps/` calls `record_relationship_event`", while the assertion at `:178-180` correctly requires `callers == ["src/my_pa/application/situation_service.py"]`. The assertion is right; the prose is not.
5. **The DB trigger's binding is not principal-scoped.** `migrations/versions/20260804_7f2a9d6c4e18_…py:255-284` joins cases to decisions with **no `principal_id` predicate**, so cross-Principal authority refusal rests entirely on the application layer. Two independent application barriers were verified experimentally, so this is defence-in-depth rather than a live gap. Pre-existing; not in this diff.
6. **`record_event(accepted=...)` is caller-supplied and bound to no review decision** — WP-11 NOTE 2's exact posture on a second surface. The row keeps no `accepted_by_review_decision_id`. Latent: the flag defaults `False`, only `situation_service` reaches the writer, and no capability reaches the plane, so no derivation can promote its own output onto an accepted timeline. **Discharge condition, named:** the new guard reddens if the default flips, if the column appears, or if a second caller arrives — wiring it forces the governance question first. Registering rather than repairing was the right call; inventing a governance path for a consumer that does not exist is what YAGNI forbids.
7. **Pre-existing cross-Principal existence disclosure, confirmed pre-existing.** `an_observed_source_version_is_recorded_once UNIQUE (source_id, source_object_id, source_version)` on `relationship_identity_observations` is **table-wide with no `principal_id`** (`20260804_7f2a9d6c4e18…:51-52`); `principal_id` was added a day later by `20260805_c1f2d3e4a5b6` **without amending that UNIQUE**. Two Principals recording the same source version collide with `IntegrityError` where absence would have succeeded. Neither migration is in this diff — this change neither introduced nor widened it — but it is a genuine leak channel needing an owning package.
8. **`relationship_events` cannot name the project it links to.** `RelationshipEventType.PROJECT_LINK` exists (`domain/relationship/event.py:54`) but the table has no `project_id` — only `context` and `source_ref` as Text. Person↔Project association is expressible only through the continuity plane (`commitments.counterparty_person_id` + `commitments.project_id`). Pre-existing WP-06 shape, outside WP-12's stated acceptance, but it weakens the "project/context association" the objective names.
9. **The time-dependent pre-existing failure is now live and permanent.** `tests/database/test_reveal_isolation.py` (1 failed + 5 cascading fixture errors) fails on every run from 2026-08-10 12:00 UTC onward, for the wall-clock reason recorded in D-21. **Every campaign baseline from here reports 2 failures + 5 errors, not 1 failure.** It needs an owning package; the fix is to make the fixture's instant relative rather than absolute.

## Relationship UX still unwired after WP-12 — enumerated precisely

WP-12 delivered the domain/persistence/Review core and deliberately left the surface unwired, per the Orchestrator's scope decision. What remains, exactly:

- `web/src/app/(app)/relationships/[personId]/page.tsx` and `web/src/components/relationship/relationship-timeline.tsx` render from `lib/fixtures/situation`, not the backend.
- `web/src/app/api/relationships/[personId]/timeline/route.ts` returns `notImplemented` in backend-serving mode and synthetic fixtures otherwise.
- **No `Capability` member exposes any relationship read or write.** `SqlRelationshipEventRepository.list_accepted_events` and the whole `SqlRelationshipRepository` are unreachable over the transport.
- **No relationship identity review UI exists at all** — no surface opens a review, decides one, or applies a resolution. Governance is repository-only.
- This is consistent with a standing operator hold the repository already states in its own plan (`docs/plans/mcv-completion-plan.md:531-533`: the Relationship Intelligence responsive surfaces "are not planned here and remain held"), not a WP-12 reclassification. The continuity write plane is unwired on identical terms (D-20).

### D-22 — WP-13 discharged the campaign's §31 browser-evidence debt, and the browser immediately found two defects 4,600 passing tests could not see

- **This is the finding that matters most, and it vindicates §31 as a control rather than a formality.** Every package from WP-06 onward honestly labelled its frontend proofs unit/integration and explicitly declined to assert end-to-end. WP-13 stood up a real Chromium suite — **69 passed / 1 skipped / 0 failed**, driving a real browser against the real Next tier, the real `apps/gateway.py`, and real PostgreSQL migrated to head on a disposable database. Within roughly an hour of the first real browser starting it surfaced **two pre-existing defects, both silent, both invisible to the entire ~4,600-test suite**:
  1. **Every signed-in page was unreachable in a browser.** Next compiles route handlers and server components into **separate module graphs**, so `session-registry.ts`'s module-level `Map` existed once per graph: `registerSession` wrote to one and `resolveSessionPrincipal` read the other. `POST /api/session` returned 200 and `GET /today` carrying that same cookie, in the same second, returned **307 to `/sign-in`**. Invisible to Vitest because it has one module graph. Fixed by keying the registry on a `Symbol.for` slot on `globalThis`. Rotation, revocation, principal-equality and the idle window are unchanged, and the fix strictly *strengthens* isolation: under the old split maps, `revokeSession` in one graph never propagated to the other.
  2. **The service worker was caching rendered, principal-bound pages.** `isCacheable` was a deny-list, but an App Router RSC navigation is a `GET` to the page's own path with `?_rsc=…` and `destination: "empty"` — so rendered pages were written into a cache **shared by every session on the browser profile**, which is precisely what that file's own docstring forbids. Replaced with an allow-list (`/_next/static/`, `/icons/`, the manifest) plus refusals for query strings, navigations, cross-origin and redirected responses; `CACHE_NAME` bumped v1→v2 with `activate` deleting every non-matching cache, so the poisoned cache is evicted.
- **Both fixes were proved non-vacuous by controlled reversion, by the independent reviewer's own hand.** Reverting `isCacheable` to the deny-list produced **5 failed / 14 passed** on the RSC, navigation and allow-list cases; reverting the registry to module-local maps produced **1 failed / 6 passed**. Both files were restored via `git checkout --` and **SHA-256 verified identical**, and both suites re-run green. The reviewer also tried to find a request shape that still slips the allow-list — RSC without `?_rsc=`, `/_next/data`, opaque/cross-origin, 206 range — and found none.
- **The time bomb is repaired, and repaired so it cannot recur.** `tests/database/test_reveal_isolation.py`'s `WHEN` is no longer a calendar literal. `_complete_derivation` now reads `SELECT now()` **on the same connection inside the same transaction**; PostgreSQL's `now()` is `transaction_timestamp()`, fixed for the transaction, so it is the identical value `started_at`'s `server_default=func.now()` will write. `completed_at == started_at` satisfies the `>=` CHECK with **no forward margin and no clock-skew assumption between client and server**. A relative instant with a fixed margin would only have moved the fuse. **Zero assertions deleted, skipped, xfailed or loosened** — the diff removes four lines total (an import, the literal, one docstring line, and `completed_at=WHEN`). Verified passing **6/6 with 0 skipped** by implementer, reviewer and Orchestrator independently, at 16:09 UTC — four hours past the expired literal, so the repair is demonstrably load-bearing rather than incidentally green.
- **Surfaces completed.** Library was a static card asserting "No sources are connected yet" **regardless of real state** — a synthetic claim, and the last one; it is now real-backed with search over `capture.list`/`capture.search`. Review and `/relationships/[personId]` previously **threw in a default build** (unguarded fixture gate) and now render honestly. System's four hardcoded claims are now derived from `capabilities.get`.
- **Truthful states are the package's core deliverable and are proved at three levels.** `lib/api/surface-answer.ts` classifies `unavailable` and `degraded` **before the row count is consulted**, so a byte-identical HTTP 200 differing only by `coverage.state` renders differently — asserted at jsdom component level, at page level with real transport, and in Chromium against a **genuinely refused connection** (a second Next server pointed at a dead port, not a route stub). The distinction reaches the accessibility tree: `role="status"` for empty versus `role="alert"` for unavailable.
- **Deliberately not done, recorded as decisions rather than omissions:** **no new capability seat and no migration** — Commitments, Decisions and Tasks remain unsurfaced because a read seat would require a migration and frozen-CHECK widening disproportionate to this package, and they still reach a human through Pulse; **no Frames surface and no separate Trace surface** — WP-09 delivers the derivation trace inside Reveal and WP-11 declined `/api/trace`, so inventing a second answer would answer a different question; **the relationship plane was not expanded**, deliberately, because WP-12 NOTEs 5/6/7 form one coupled obligation that activates the moment any capability seat reaches that plane.
- **Evidence:** head `f68478d3121a3d7a6983c0cdbe09d49aba1741b5`, PR **#66**, stacked on #65, base `3fbbd22b`. Full suite **4615 passed / 1 failed / 0 errors** against the base's **4609 / 2 / 5 errors**; `tests/schema tests/database` **285 / 0 / 0** against 279 / 1 / 5; architecture **2017 / 1**; web unit **282 passed / 26 files** against 246 / 24, **0 skipped**; browser E2E **69 / 1 skipped / 0 failed**; ruff, format and mypy clean over **177**; lint, typecheck and build clean. Single Alembic head **`8f2b6c4d1a37`** over 26 revisions, capability seats still **19**, and `git diff base..head -- src/ apps/ migrations/ schemas/` is **empty**. Arithmetic closes exactly: 4609 + 6 = 4615, and 279 + 6 = 285. **Independently reviewed at the exact head: clean, zero BLOCKERs, nine NOTEs**, every claimed figure reproduced by the reviewer's own hand.
- **The "246 / 24" figure quoted from WP-09 onward was misread as 24 skipped tests. It is 246 tests across 24 test *files*.** There have never been skipped Vitest tests in this campaign.
- **Level of proof, stated exactly and not to be upgraded in any later restatement.** Real-backing, truthful states, responsive layout, WCAG-automated checks and PWA install criteria are proved **in a real browser**. Accessibility is `axe-core` in Chromium under `wcag2a wcag2aa wcag21a wcag21aa wcag22aa`: **0 violations** across sign-in, all five destinations and the capture dialog — but automated scanning covers roughly the machine-checkable third of WCAG, so this is a **target, not a conformance claim**. **Account isolation is proved at unit/integration/API level only.** No two-identity browser test exists and **none is constructible at this head**, because D-15 pins the web tier to exactly one Principal under `local_operator`; the pin was **not widened** to manufacture one, and WP-08 NOTE 1's discharge condition was **not created**. `metadata.principal_id` is still read nowhere in production — D-12 remains vacuum-sealed.
- **Invalidation:** canonical WP-13's own rule — material UX/navigation/auth changes invalidate browser acceptance.

## NOTEs carried out of WP-13 (§15A — recorded, not blocking)

The independent review returned **zero BLOCKERs and nine NOTEs**. **Eight were closed by a correction Worker** before the package shipped, because they undercut this package's own two central claims — that empty, unavailable and degraded are truthfully distinguished, and that the browser evidence is honest and non-vacuous. What was closed, and what remains:

**Closed in-package (C1–C8).** Situations asserted emptiness under a *partial* answer while Today/Library/Review all guarded it (the correction Worker also found and fixed the narrower case where one half degrades beside a healthy other half, and flagged the widening rather than folding it in silently); an always-true `expect(objectLiteral).toBeTruthy()`; a loop whose body executed zero times against a healthy stack; a test whose name promised an idempotency proof its body did not make (now replayed against the real gateway and real `UNIQUE (principal_id, idempotency_key)`); a PWA cache assertion that filtered only `/api` and so **would not have caught the RSC defect this package fixed**; a title/threshold disagreement on touch targets; `?? 0` rendering an invented "0 of 0 contracted capabilities"; and a `try`/`catch` that suppressed an auth-mode misconfiguration into a benign default. Each new guard was run against the pre-fix files by controlled reversion and **observed red**.

**Carried forward:**

1. **`Symbol.for` is per-realm.** `worker_threads` or multiple Node processes still get separate session registries. Not a regression — strictly better than the per-module-graph state it replaced — and the module already documents itself as process-local with a named swap point for a durable store. Discharge when the web tier runs more than one process.
2. **The browser suite runs against `next dev`, not a production build.** This is forced, not chosen: `lib/auth/mode.ts` refuses the synthetic provider (the only sign-in this build implements) under `NODE_ENV=production`, and a production-mode browser run needs a real Entra sign-in that does not exist. `npm run build` passes separately. **This is the one tier configuration nothing in this campaign has ever exercised, and is the next place to look for defects of the kind §31 just found.**
3. **The E2E needed a route warm-up** (`e2e/global-setup.ts`). Without it: 55 passed / 16 failed with scattered sign-in failures; with it, green in 1.4m. A dev server compiling on demand reloads its module context and drops the in-memory registry. A dev-mode artefact, **named rather than retried away — no retries are configured.**
4. **`OfflineQueueStatus` does not refresh when a note is queued** into an already-mounted page; it drains on mount and on `online` only. The *primary* statement in the capture dialog is correct and asserted; the secondary badge is stale until the next drain. Recorded in `e2e/offline.spec.ts`, not asserted away.
5. **`/library?q=` search is exact-token and unstemmed** — a backend property, disclosed in the UI rather than hidden.

## The unowned architecture failure — now the only red in the suite, with a specified fix

**WP-12 NOTE 9 is CLOSED by WP-13's repair.** The time bomb no longer reddens any run and the 5 cascading fixture errors are gone. **Baselines from here report 1 failure, not 2 failures + 5 errors.**

That single remaining failure is `test_every_python_root_is_type_checked_or_named` (WP-04 NOTE 6, WP-06 NOTE 9, WP-07 NOTE 9, WP-11 NOTE 3), and it has now survived ten packages unowned. It is **confirmed pre-existing** by three independent measurements: the only `.py` under `web/` is untracked `web/node_modules/flatted/python/flatted.py`; `flatted` is in the **base** `package-lock.json` (via `flat-cache` → eslint); and WP-13's diff touches none of the test's inputs. It requires `npm install` to reproduce — a clean checkout reports **0 failed**.

**The reviewer assessed the fix as safe, bounded, and rule-*strengthening*, and it is specified here so the next owner does not have to re-derive it.** One file, one function: `tests/architecture/test_ci_invokes_mypy_over_the_declared_tree.py:714-745`. The defect is the hand-rolled `.gitignore` parser at `:729-734`, which understands only top-level, slash-free patterns from the **root** file, so `web/node_modules` — ignored by `web/.gitignore` — is not excluded and `web` becomes a phantom Python root.

- **Preferred (~15 lines net):** derive roots from `git ls-files -- '*.py'` instead of `ROOT.glob("*/**/*.py")`, deleting the parser entirely. A vendored `.py` inside *any* ignored directory can then never manufacture a phantom root. Costs an `import subprocess` and would not work from a non-git export.
- **Smaller (~5 lines):** also read nested `.gitignore` files for directory patterns. Dependency-free, slightly weaker.

Either touches no production code and no other test. **WP-13 deliberately did not take it**, because the operator's assignment named this failure explicitly and assigned only the time bomb — widening scope on a package the operator had already scoped would have been the wrong call.

## Library is no longer unwired — WP-09 NOTE 3 is closed

`web/src/app/(app)/library/page.tsx` rendered a static `NotConnected` card asserting "No sources are connected yet" independent of real state. WP-09 raised it as a NOTE, WP-11 ruled it out of scope, and WP-13 wired it. **It was the campaign's last standing synthetic claim on a real-backed capability.** Today's and Situations' `NotConnected` cards were legitimate stated-failure paths but were badged "Not yet connected", which reads as a missing feature rather than a failed read; they now use the four-state components.
