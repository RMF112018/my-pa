# Source Tree

**Status:** `SCAFFOLD_ONLY`

This directory contains the installable Python source namespace. All product code belongs under `src/my_pa/`; new top-level Python namespaces require an explicit architecture decision.

The source tree is currently documentation-only. It contains no executable package, configuration, database connection, migration, connector, worker, or deployment behavior.
