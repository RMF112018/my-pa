import { FeatureRouteState } from "@/components/shell/feature-route-state";
export const metadata = { title: "Intelligence — my-pa" };
export default function IntelligencePage() { return <FeatureRouteState title="Intelligence" description="Evidence-grounded reports and briefs." state="unavailable" detail="The normalized report plane is not admitted to current main. Open PR #139 is candidate evidence only, so this shell does not present it as available." />; }
