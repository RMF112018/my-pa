import { PeopleEntityPage } from "@/app/(app)/people/[entityId]/people-entity-page";

export const metadata = { title: "Person — my-pa" };
export const dynamic = "force-dynamic";
export default PeopleEntityPage;
